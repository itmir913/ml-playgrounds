# CLAUDE.md — ML Playgrounds

> 이 저장소에서 작업할 때 **가장 먼저 읽는 문서**다. 여기 담긴 것은 **규칙**이다.
>
> **사실은 여기 적지 않는다.** 에러 코드 목록, 상한값, 디렉터리 구조, JSON 스키마는
> 코드가 유일한 출처다. 문서에 복사해 두면 반드시 어긋난다.
>
> 원칙과 충돌하는 코드는 작성하지 마라. 원칙을 바꿔야 한다고 판단되면
> 코드를 고치기 전에 사용자에게 먼저 확인하라.

---

## 0. 무엇을 만드는가

**ML Playgrounds** — 학생이 데이터를 올려 머신러닝 모델을 만들고, 결과를 분석하고,
포트폴리오를 작성해 **하나의 프로젝트 파일**로 저장·제출·공유하는 교육용 AI 플랫폼.

- 대상: 중·고등학교 정보/인공지능 수업의 교사와 학생
- 전제: 전국 단위 배포. 다수의 동시 사용자, 다양한 서버 환경.
- 포지션: Orange3(워크플로 편집기)와 Teachable Machine(즉석 모델 생성)이 놓치는
  "학생이 프로젝트를 만들고 그 과정을 남기는 경험"을 제공한다.
- 저장소: `https://github.com/itmir913/ml-playgrounds` · 라이선스: MIT

```
새 프로젝트 → 데이터 준비 → 모델 학습 → 결과 분석 → 포트폴리오 작성 → 프로젝트 저장
```

모든 활동은 하나의 프로젝트 안에서 일어나며, 결과물은 파일 하나다.

---

## 1. 절대 원칙 (Non-negotiables)

이 다섯 가지는 기능 추가보다 우선한다. 위반하는 PR은 거부한다.

### 1.1 서버는 계산만 한다

- 서버는 사용자 계정, 프로젝트, 데이터셋, 학습 결과, 모델을 **영구 저장하지 않는다**.
- 서버가 데이터를 보유하는 기간은 **세션이 살아 있는 동안**이다.
  **세션의 수명 = WebSocket 연결의 수명.** 연결이 끊기면 그 세션의 모든 것을 삭제한다.
  (설정을 바꿔가며 반복 학습하는 것이 이 도구의 핵심 활동이다. 매번 재업로드를 요구하면 도구가 쓸모없어진다.)
- 세션에도 상한이 있다 — 유휴 타임아웃, 세션당 디스크, 동시 세션 수. (§1.5)
- 디스크에 남는 것은 없어야 한다. 재부팅하면 서버는 백지 상태로 돌아온다.
- 서버는 학생의 인적사항(이름, 학번, 학교 등)을 **받지 않는다**. 업로드되는 것은 순수 학습 데이터뿐이다.
- 예외: 운영에 필요한 최소 로그(IP, 타임스탬프, 작업 유형, 상태 코드). 정책은 `docs/privacy.md`.
  (수집 범위와 보존 기간은 미확정 → `docs/open-decisions.md`)

### 1.2 브라우저가 저장소다

- 프로젝트 보관·자동 저장·불러오기는 IndexedDB에서 이루어진다.
- 학생 데이터의 소유권은 학생에게 있다. 서버가 없어도 학생은 자기 프로젝트를 가지고 있어야 한다.

### 1.3 하나의 프로젝트 = 하나의 파일

- `.mlproj` 파일 하나가 프로젝트이자 수행평가 제출물이자 포트폴리오다. 실체는 zip이다.
- 교사는 이 파일 하나만 열면 **재학습 없이** 모든 것을 볼 수 있어야 한다.
- 확장자 문자열은 **상수 하나로 관리**한다. 코드에 흩뿌리지 마라.

### 1.4 백엔드에 사람이 읽는 문자열은 없다

- 백엔드는 **상태 코드와 구조화된 파라미터만** 반환한다.
- 언어를 막론하고 UI에 그대로 노출될 자연어 문장을 백엔드가 만들지 않는다. 규칙은 §3.
- **이것은 다국어 지원의 전제이며, 나중에 고치기 가장 어려운 부분이다.**

### 1.5 자원은 유한하다

- 운영 서버는 저장 공간과 메모리가 넉넉하지 않다.
- 업로드를 **받기 전에** 가용 자원을 확인하고, 부족하면 거부한다.
- 모든 작업에 시간·메모리·크기 상한이 있다. 상한은 전부 환경 변수로 조정 가능해야 한다.

---

## 2. 스택과 설계 원칙

**프런트엔드** Vue 3 (`<script setup>`) · TypeScript strict · Tailwind · Pinia · vue-i18n · IndexedDB · Vite
**백엔드** Python 3.11+ · FastAPI · Pydantic v2 · scikit-learn / pandas / numpy
**배포** Docker Compose (단일 호스트에서 시작)

교체가 예정된 세 지점은 **처음부터 인터페이스 뒤에 둔다.** 구현 교체가 코드 전체에 파급되면 안 된다.

| 지점 | 인터페이스 | V1 구현 | 이후 |
|---|---|---|---|
| 작업 큐 | `JobQueue` | `InProcessQueue` | `CeleryQueue` (Redis) |
| 학습 실행 | `TrainingBackend` | 서버 학습 | 브라우저 학습 (Pyodide) |
| 알고리즘 | `registry` 등록 | — | 등록만 추가 |

- 알고리즘 선택에 `if/elif` 분기 금지. 반드시 registry 등록 방식으로.
- `randomState`는 **항상 저장하고 항상 사용한다.** 재현 가능성이 교육용 도구의 생명이다.

상세(큐 라우팅, 자원 관리 정책, 데이터 수명주기, 디렉터리 구조) → `docs/architecture.md`

---

## 3. 국제화 (i18n)

지원 언어는 배열로 관리한다. **2개 언어(en/ko) 전용 분기를 만들지 마라.** ja가 추후 추가된다.

**백엔드**

1. 사람이 읽는 문장을 **절대 반환하지 않는다.** 오직 코드와 파라미터다.
2. 에러 응답 형식은 전 API가 동일하다 — `{"error": {"code": "...", "params": {...}}}`
   WebSocket 진행 이벤트도 같은 원칙 — `{"jobId": "...", "stage": "...", "progress": 0.42}`
3. 예외: **사용자 데이터에서 온 값**(CSV 컬럼명, 클래스 라벨)은 그대로 통과시킨다. UI 문자열이 아니라 데이터다.
4. 코드 목록은 `backend/app/errors.py`의 `StrEnum` 하나가 **유일한 출처**다.
   명명 규칙은 `{도메인}_{문제}` 대문자 스네이크.
5. 예외는 `AppError(code, params)` 하나로 통일하고, 핸들러가 위 형식으로 직렬화한다.

**프런트엔드**

1. `frontend/src/locales/{en,ko}.json`. 키는 백엔드 코드와 1:1 대응한다.
2. 컴포넌트 안에 자연어 문자열 리터럴 금지. 전부 `t()`를 거친다. 포트폴리오 템플릿 문구도 마찬가지다.
3. 숫자·날짜·백분율은 `Intl` API로 포맷팅한다. 직접 문자열 조립 금지.
4. 언어 선택은 사용자가 명시적으로 바꿀 수 있고, 선택은 IndexedDB에 저장된다. 초기값은 `navigator.language` 기반.

**CI로 강제한다.** 사람이 규칙을 지키길 기대하지 않는다.

- `backend/app/` 의 `.py` **문자열 리터럴**에 한글이 있으면 실패.
  주석과 docstring은 예외다 — 한국어로 써도 된다. 막으려는 건 UI로 새어 나가는 문자열이지 개발자용 설명이 아니다.
  (구현: `ast`로 문자열 상수만 순회한다. 주석은 애초에 AST에 없다.)
- `en.json`과 `ko.json`의 키 집합이 다르면 실패
- `errors.py`의 모든 코드가 각 로케일 파일에 존재하지 않으면 실패

새 에러 코드를 추가하면 **같은 커밋에서** `en.json`, `ko.json`, `docs/error-codes.md`를 함께 갱신한다.

---

## 4. 개발 규약

- 백엔드: 타입 힌트 필수, `ruff`(린트+포맷), `mypy` strict, `pytest`. 모든 API 응답은 Pydantic 모델.
- 프런트엔드: ESLint + Prettier, `vitest`. `any` 금지 — 불가피하면 이유를 주석으로 남긴다.
- 커밋: Conventional Commits (`feat:` `fix:` `docs:` `refactor:` `test:` `chore:`)

**커밋 규칙 (예외 없음)**

- **`main`에서 순차적으로 작업한다.** 기능 브랜치를 만들지 마라.
  병렬 브랜치는 토큰만 쓰고 이 프로젝트 규모에서 얻는 게 없다.
- **모든 커밋은 GPG 서명한다.** `--no-gpg-sign`이나 `-c commit.gpgsign=false`를 쓰지 마라.
  서명이 실패하면 우회하지 말고 그대로 멈추고 원인을 보고하라.
- **커밋 메시지에 `Co-Authored-By`, `Generated with` 같은 도구 표기를 넣지 마라.**
  누가·무엇이 썼는지는 커밋 메시지에 남기지 않는다. 메시지에는 변경 내용만 적는다.
- **스테이징 경로를 반드시 명시한다.** 동시에 병렬 세션이 돌면 커밋이 섞인다.
  `git add .`, `git add -A`, `git commit -a` 전부 **금지**.
  - 추적 중인 파일: `git commit -- <경로1> <경로2>`
  - 새 파일: `git add -- <경로1> <경로2>` 로 **하나씩 열거한 뒤** 같은 경로로 커밋한다.
    (`git commit -- <경로>`는 인덱스에 없는 파일에는 동작하지 않는다)

**개발 방식 (이 저장소의 작업 리듬)**

- **백엔드는 테스트를 먼저 쓴다.** 거의 모든 구현이 테스트와 함께 온다.
  테스트 없는 백엔드 코드를 제안하지 마라.
- **프런트엔드는 두 갈래다.** 눈으로 확인해야 하는 화면은 브라우저에서 직접 조작해 검증한다.
  로직(포맷 파싱, 마이그레이션, IndexedDB, 스토어, 컴포저블)은 vitest로 덮는다.
  화면을 만들 때는 검증 가능한 로직을 컴포넌트 밖으로 빼서 테스트할 수 있게 하라.
- IDE 실행 구성은 `.idea/runConfigurations/`의 `dev` / `build` / `test` 세 개다.

**테스트에서 반드시 다뤄야 할 것**

- `.mlproj` 저장 → 열기 왕복 무손실 / `formatVersion` 마이그레이션
- 무결성 서명 생성 → 검증 → 변조 탐지
- 상한 초과 시 올바른 에러 코드 반환 / 디스크 부족 시 업로드 거부
- 작업 완료·실패·취소·타임아웃 **모든 경로**에서 임시 파일이 삭제되는지
- 백엔드 응답에 자연어 문자열이 하나도 없는지

---

## 5. 상세 문서

필요할 때 읽어라. 이 문서에 내용을 다시 옮기지 마라.

| 문서 | 언제 읽는가 |
|---|---|
| `docs/architecture.md` | 큐·워커·자원 관리·디렉터리 구조를 건드릴 때 |
| `docs/mlproj-spec.md` | 프로젝트 파일 포맷·무결성 서명을 건드릴 때 |
| `docs/roadmap.md` | 이 기능이 지금 범위인지 확인할 때 |
| `docs/open-decisions.md` | **구현 전 반드시 확인.** 아직 결정되지 않은 항목 |
| `docs/error-codes.md` | 에러 코드 레퍼런스 (출처는 `errors.py`) |
| `docs/privacy.md` | 로깅·개인정보 정책 |

---

## 6. 작업 시 자문할 것

- **"이게 서버에 상태를 남기는가?"** 남긴다면 설계가 잘못됐다. (§1.1)
- **"이 문자열이 백엔드에서 오는가?"** 온다면 코드로 바꿔라. (§1.4)
- **"이 값이 CLAUDE.md에도 적혀 있는가?"** 그렇다면 문서에서 지워라. 코드가 출처다.
- **잘 모르겠으면 추측해서 구현하지 마라.** `docs/open-decisions.md`에 항목을 추가하고 물어라.

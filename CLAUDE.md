# CLAUDE.md — ML Playgrounds

> 이 저장소에서 작업할 때 **가장 먼저 읽는 문서**다. 여기 담긴 것은 **규칙**이다.
>
> **사실은 여기 적지 않는다.** 에러 코드 목록, 상한값, 디렉터리 구조, JSON 스키마는
> 코드가 유일한 출처다. 문서에 복사해 두면 반드시 어긋난다.
>
> 원칙과 충돌하는 코드는 작성하지 마라. 원칙을 바꿔야 한다고 판단되면
> 코드를 고치기 전에 사용자에게 먼저 확인하라.

---

## 0. 무엇을 만드는가

**ML Playgrounds** — 학생이 데이터를 올려 머신러닝 모델을 만들고, 결과를 분석하고,
포트폴리오를 작성해 **하나의 프로젝트 파일**로 저장·제출·공유하는 교육용 AI 플랫폼.

- 대상: 중·고등학교 정보/인공지능 수업의 교사와 학생
- 전제: 전국 단위 배포. 다수의 동시 사용자, 다양한 서버 환경.
  **기기도 다양하다 — 학교 PC, 가정 PC, 휴대폰.** 무거운 것을 넣을지 판단할 때
  기준은 저사양 학교 PC와 휴대폰이다 (`docs/open-decisions.md` "모바일에서도 동작한다").
- 포지션: Orange3(워크플로 편집기)와 Teachable Machine(즉석 모델 생성)이 놓치는
  "학생이 프로젝트를 만들고 그 과정을 남기는 경험"을 제공한다.
- 저장소: `https://github.com/itmir913/ml-playgrounds` · 라이선스: MIT

```
새 프로젝트 → 데이터 준비 → 모델 학습 → 결과 분석 → 포트폴리오 작성 → 프로젝트 저장
```

모든 활동은 하나의 프로젝트 안에서 일어나며, 결과물은 파일 하나다.

---

## 1. 절대 원칙 (Non-negotiables)

이 다섯 가지는 기능 추가보다 우선한다. 위반하는 PR은 거부한다.

### 1.1 서버는 없거나, 있어도 계산만 한다

**공식 배포에는 서버가 없다.**

- 우리가 공개하는 것은 **GitHub Pages 정적 페이지 하나**뿐이다. 데이터 업로드·학습·예측·저장이
  전부 브라우저에서 끝난다.
- 백엔드는 **구현하되 우리가 공개 호스팅하지 않는다.** 도커 이미지로 배포하고,
  더 강한 연산이 필요한 학교가 컴퓨터실에 직접 띄운다.
- **왜 공용 서버가 불가능한지는 `docs/architecture.md` §7.1에 실측과 함께 있다.**
  요약하면 전국 단위로는 어떤 계산으로도 못 버티고, 막으려고 계정을 두면 아래 조항과
  충돌하며, **수행평가 도중 서버가 죽으면 그 실패는 교사가 뒤집어쓴다.**

그러므로 **실행 위치는 브라우저가 기본이고 서버는 예외다.** 서버가 없어도 모든 기능이
동작해야 한다. 서버를 전제하는 기능은 만들지 마라.

**서버가 존재하는 경우(자가호스팅)에도 계산만 한다.**

- 서버는 사용자 계정, 프로젝트, 데이터셋, 학습 결과, 모델을 **영구 저장하지 않는다**.
- 서버가 데이터를 보유하는 기간은 **세션이 살아 있는 동안**이다.
  **세션의 수명 = WebSocket 연결의 수명.** 연결이 끊기면 그 세션의 모든 것을 삭제한다.
  (설정을 바꿔가며 반복 학습하는 것이 이 도구의 핵심 활동이다. 매번 재업로드를 요구하면 도구가 쓸모없어진다.)
- 세션에도 상한이 있다 — 유휴 타임아웃, 세션당 디스크, 동시 세션 수. (§1.5)
- 디스크에 남는 것은 없어야 한다. 재부팅하면 서버는 백지 상태로 돌아온다.
- 서버는 학생의 인적사항(이름, 학번, 학교 등)을 **받지 않는다**. 업로드되는 것은 순수 훈련 데이터뿐이다.
- 예외: 운영에 필요한 최소 로그(IP, 타임스탬프, 작업 유형, 상태 코드). 정책은 `docs/privacy.md`.
  (수집 범위와 보존 기간은 미확정 → `docs/open-decisions.md`)

### 1.2 브라우저가 저장소다

- 프로젝트 보관·자동 저장·불러오기는 IndexedDB에서 이루어진다.
- 학생 데이터의 소유권은 학생에게 있다. 서버가 없어도 학생은 자기 프로젝트를 가지고 있어야 한다.
- **다만 학교 컴퓨터실에서는 이것이 한 차시를 넘지 못한다.** 전원을 끄면 디스크가 되돌아간다.
  거기서 IndexedDB가 하는 일은 새로고침·크래시 복구까지이고, 차시를 건너는 것은 `.mlpx`다.
  → `docs/open-decisions.md` "컴퓨터실 PC는 리셋을 전제한다"

### 1.3 하나의 프로젝트 = 하나의 파일

- `.mlpx` 파일 하나가 프로젝트이자 수행평가 제출물이자 포트폴리오다. 실체는 zip이다.
- 교사는 이 파일 하나만 열면 **재학습 없이** 모든 것을 볼 수 있어야 한다.
- 확장자 문자열은 **상수 하나로 관리**한다. 코드에 흩뿌리지 마라.

### 1.4 백엔드에 사람이 읽는 문자열은 없다

- 백엔드는 **상태 코드와 구조화된 파라미터만** 반환한다.
- 언어를 막론하고 UI에 그대로 노출될 자연어 문장을 백엔드가 만들지 않는다. 규칙은 §3.
- **이것은 다국어 지원의 전제이며, 나중에 고치기 가장 어려운 부분이다.**

### 1.5 자원은 유한하다

- 운영 서버는 저장 공간과 메모리가 넉넉하지 않다.
- 업로드를 **받기 전에** 가용 자원을 확인하고, 부족하면 거부한다.
- 모든 작업에 시간·메모리·크기 상한이 있다. 상한은 전부 환경 변수로 조정 가능해야 한다.
- **크기·개수·시간 상한을 코드에 직접 쓰지 마라.** 백엔드는 `config.py`의 환경 변수,
  프런트엔드는 `frontend/src/limits.ts`의 이름 있는 상수가 유일한 출처다.
  상수 하나만 바꾸면 전부 반영돼야 한다. 값 자체는 여기 적지 않는다 — 코드가 출처다.

---

## 2. 스택과 설계 원칙

**프런트엔드** Vue 3 (`<script setup>`) · TypeScript strict · Tailwind · Pinia · vue-i18n · IndexedDB · Vite
**백엔드** Python 3.12+ · FastAPI · Pydantic v2 · scikit-learn / pandas / numpy
**배포** GitHub Pages 정적 배포(공식) + 도커 자가호스팅(선택). 상세는 `docs/open-decisions.md` #10

교체가 예정된 세 지점은 **처음부터 인터페이스 뒤에 둔다.** 구현 교체가 코드 전체에 파급되면 안 된다.

| 지점 | 인터페이스 | V1 구현 | 이후 |
|---|---|---|---|
| 작업 큐 | `JobQueue` (`backend/app/jobs/base.py`) | `InProcessQueue` | `CeleryQueue` (Redis) |
| 실행 위치 | `ml/backend.ts`의 판정 함수 | **브라우저** | 서버 (자가호스팅) |
| 학습 엔진 | `ml/engines`의 등록부 | 순수 JS | sklearn·서버는 등록만 추가 |
| 알고리즘 | `registry` 등록 | — | 등록만 추가 |

- **이름·순서·기본값에서 고를 것이 있으면 파이썬 생태계의 관행을 고른다.** 이 도구는
  종착지가 아니라 **scikit-learn으로 가는 발판**이고, 화면에서 익힌 모양이 거기서 그대로
  통해야 여기서 한 일이 남는다 (`docs/open-decisions.md` "파이썬 표준 관행을 따른다").
  **낱말은 학계에서 굳어진 말을, 구조는 표준 라이브러리를 따른다.**
  **우리가 지어낸 말은 쓰지 않는다** — 여기서만 통하는 낱말은 학생이 다음에 갈 곳에서
  통하지 않고, 그러면 여기서 배운 것이 남지 않는다. 용어는 `docs/terms.md`가 갖는다.
- 알고리즘 선택에 `if/elif` 분기 금지. 반드시 registry 등록 방식으로.
- **"X는 Y에서만 쓸 수 있다"는 X의 등록부 항목에 적는다. Y의 화면에 적지 마라.**
  지표·전처리 단계도 알고리즘과 같다. 상세와 강제 장치는 `docs/architecture.md` §9.
- **잠기는 것에는 gate 함수가 하나 있고, boolean이 아니라 이유 목록을 반환한다.**
  템플릿에서 조건을 조립하지 마라 (§10). `tests/ui-rules.spec.ts`가 막는다.
- `randomState`는 **항상 저장하고 항상 사용한다.** 재현 가능성이 교육용 도구의 생명이다.

**배포 경로는 둘이고, 빌드 산출물은 하나다.**

| 경로 | 무엇이 도는가 | 누가 운영하나 |
|---|---|---|
| GitHub Pages (공식) | 정적 프런트엔드만. 학습은 브라우저 | 아무도 — 정적 호스팅 |
| 도커 (자가호스팅) | 프런트엔드 + 백엔드를 **같은 오리진**에서 | 설치한 학교·교사 |

같은 `dist/`가 양쪽에 그대로 들어간다. **백엔드 주소를 설정 가능하게 만들지 마라** —
같은 오리진이 아니면 CORS와 mixed content와 교사가 고쳐야 할 설정 파일이 한꺼번에 생긴다.

화면은 항상 실행 위치를 함께 보여주고, 서버에 연결되지 않았으면 서버 옵션을 **이유와 함께**
비활성화한다.

상세(배포 판정, 큐 라우팅, 자원 관리, 데이터 수명주기, 화면 설계) → `docs/architecture.md`

---

## 3. 국제화 (i18n)

**지원 언어는 배열로 관리한다. 2개 언어(en/ko) 전용 분기를 만들지 마라.** ja가 추후 추가된다.

지켜야 하는 것은 넷이고, 나머지는 전부 `docs/i18n.md`에 있다.

1. **백엔드는 코드와 파라미터만 반환한다** (§1.4). 형식·명명·예외 규약은 `docs/i18n.md`.
2. **컴포넌트 안에 자연어 문자열 리터럴 금지.** 전부 `t()`를 거친다.
3. **한 문장은 한 키다.** 조각을 이어 붙이지 마라 — 어순은 언어마다 다르다.
4. **사용자 데이터는 문장 끝에 괄호로 뺀다.** 조사가 앞 글자에 따라 갈리기 때문이다.

**CI가 강제한다.** 무엇을 막는지, 그리고 **무엇을 못 막는지**는 `docs/i18n.md`.

---

## 4. 개발 규약

- 백엔드: 타입 힌트 필수, `ruff`(린트+포맷), `mypy` strict, `pytest`. 모든 API 응답은 Pydantic 모델.
- 프런트엔드: ESLint + Prettier, `vitest`. `any` 금지 — 불가피하면 이유를 주석으로 남긴다.
- **화면 코드는 Tailwind 기본 클래스만 쓴다.** 임의 값(`w-[327px]`)을 템플릿에 두지 마라.
  꼭 필요하면 `styles/utilities.css`에 `@utility`로 한 번만 정의하고 이름으로 쓴다.
- **가장 작은 글자가 `text-base`다.** 이 도구를 쓰는 사람은 중고등학생이고 교실 모니터는
  좋지 않다. 촘촘함은 글자를 줄여서가 아니라 여백으로 얻는다.
- **오래 걸리는 버튼은 `AppButton`의 `action`으로 준다.** `@click`은 리스너의 반환값을
  Vue가 기다려 주지 않아 **두 번 눌리는 것을 막지 못한다.**

  **화면 규칙은 `frontend/tests/ui-rules.spec.ts`가 강제한다.** 무엇을 막는지는 그 파일이,
  **왜 그런지는 `docs/architecture.md` §8**이 답한다. 사람이 지키길 기대하지 않는다.
- **남의 코드를 저장소에 들이면 파일 머리에 셋을 적는다** — 출처(저장소·버전·파일),
  라이선스, **우리가 바꾼 것 전부.** 바꾼 목록이 없으면 다음 사람은 원본과 대조할 수
  없고, 그 순간 그 코드는 우리 것도 남의 것도 아니게 된다. 벤더링은 마지막 수단이다:
  먼저 의존성으로 쓸 수 없는 이유가 `docs/open-decisions.md`에 있어야 한다.
  (예: `frontend/src/ml/engines/svm-smo.ts`)
- 커밋: Conventional Commits (`feat:` `fix:` `docs:` `refactor:` `test:` `chore:`)

**커밋 규칙 (예외 없음)**

- **`main`에서 순차적으로 작업한다.** 기능 브랜치를 만들지 마라.
  병렬 브랜치는 토큰만 쓰고 이 프로젝트 규모에서 얻는 게 없다.
- **모든 커밋은 GPG 서명한다.** `--no-gpg-sign`이나 `-c commit.gpgsign=false`를 쓰지 마라.
  서명이 실패하면 우회하지 말고 그대로 멈추고 원인을 보고하라.
- **커밋 메시지에 `Co-Authored-By`, `Generated with` 같은 도구 표기를 넣지 마라.**
  누가·무엇이 썼는지는 커밋 메시지에 남기지 않는다. 메시지에는 변경 내용만 적는다.
- **스테이징 경로를 반드시 명시한다.** 동시에 병렬 세션이 돌면 커밋이 섞인다.
  `git add .`, `git add -A`, `git commit -a` 전부 **금지**.
  - 추적 중인 파일: `git commit -- <경로1> <경로2>`
  - 새 파일: `git add -- <경로1> <경로2>` 로 **하나씩 열거한 뒤** 같은 경로로 커밋한다.
    (`git commit -- <경로>`는 인덱스에 없는 파일에는 동작하지 않는다)

**개발 방식 (이 저장소의 작업 리듬)**

- **문서 갱신이 코드보다 먼저다.** 결정이 걸린 작업은 `docs/`를 고쳐 커밋한 뒤 구현
  커밋을 낸다. 순서를 뒤집으면 구현하면서 마주친 판단이 문서에 안 남고, 다음 사람은
  왜 그 모양인지 알 수 없다. 명세에 경계를 먼저 못 박아 두면 **구현하면서 흔들릴 자리가
  미리 닫힌다.** 결정이 안 걸린 순수한 버그 수정에는 해당하지 않는다.
- **백엔드는 본 코드와 테스트를 함께 쓴다.** 테스트 없는 백엔드 코드를 내놓지 마라.
  테스트를 먼저 쓰라는 뜻이 아니다. 구현과 검증이 같은 작업 단위로 묶여 있어야 한다는 뜻이다.
- **프런트엔드는 두 갈래다.** 눈으로 확인해야 하는 화면은 브라우저에서 직접 조작해 검증한다.
  로직(포맷 파싱, 마이그레이션, IndexedDB, 스토어, 컴포저블)은 vitest로 덮는다.
  화면을 만들 때는 검증 가능한 로직을 컴포넌트 밖으로 빼서 테스트할 수 있게 하라.
  **다만 눈으로 확인하는 쪽은 사용자가 자기 브라우저로 직접 본다.** Claude Code의
  브라우저 도구(Claude Browser 등 프리뷰 자동화)는 토큰을 많이 쓴다 — 사용자가
  명시적으로 켜 달라고 할 때만 쓰고, 화면을 고칠 때마다 스스로 띄워 스크린샷을 찍지
  마라.
- **커밋마다 `npm run ci`를 전부 돌린다.** 나눠 돌리거나 나중으로 미루지 마라 —
  `ui-rules`·`i18n-usage`·`locales`는 소스 전체를 훑으므로 "내가 만진 파일의 검사"라는
  것이 없고, 병렬 부하에서만 무너지는 검사도 있었다. **예외는 트립와이어 검증 하나다** —
  일부러 망가뜨려 검사가 우는지 볼 때는 그 스펙 파일 하나만 돌린다. 전체를 돌리면
  여러 개가 같이 빨개져서 **겨눈 검사가 잡았는지 구분이 안 된다.**
- **확인은 `npm run ci`(안에서 `lint:check`), 고치는 것은 `npm run lint`다.**
  `lint`는 `src/ tests/` 전체를 고쳐 쓰므로 **병렬 세션이 돌 때 남의 작업 파일까지
  건드린다.** 확인하려던 것뿐이라면 절대 `lint`를 쓰지 마라.
- **DOM이 필요한 스펙은 첫 줄에 `// @vitest-environment jsdom`으로 스스로 밝힌다.**
  기본 환경은 `node`다. 안 밝히면 `document is not defined`로 그 자리에서 죽는다.
- IDE 실행 구성은 `.idea/runConfigurations/`에 있다. 앞뒤를 함께 띄우는 것(`dev`)과
  한쪽만 띄우는 것이 나뉘어 있으니 목록은 그 디렉터리를 봐라.

**테스트에서 반드시 다뤄야 할 것**

- `.mlpx` 저장 → 열기 왕복 무손실 / `formatVersion` 마이그레이션
- 무결성 해시 생성 → 확인 → 변조 탐지 / 재실행 대조가 지표를 재현하는지
- 상한 초과 시 올바른 에러 코드 반환 / 디스크 부족 시 업로드 거부
- 작업 완료·실패·취소·타임아웃 **모든 경로**에서 임시 파일이 삭제되는지
- 백엔드 응답에 자연어 문자열이 하나도 없는지

---

## 5. 상세 문서

필요할 때 읽어라. 이 문서에 내용을 다시 옮기지 마라.

| 문서 | 언제 읽는가 |
|---|---|
| `docs/architecture.md` | **화면을 만들 때(§8)**, **능력을 더하거나 잠금을 건드릴 때(§9·§10)**, 큐·워커·자원 관리를 건드릴 때 |
| `docs/mlpx-spec.md` | 프로젝트 파일 포맷·무결성 확인을 건드릴 때 |
| `docs/roadmap.md` | 이 기능이 지금 범위인지 확인할 때 |
| `docs/open-decisions.md` | **구현 전 반드시 확인.** 미결정 절만 통독하고, 결정됨은 가리킬 때 편다 |
| `docs/i18n.md` | **로케일 파일을 건드릴 때.** §3의 상세 — 규칙 아홉과 CI가 막는 것 |
| `docs/terms.md` | **낱말을 고를 때.** 이 프로젝트가 쓰는 용어와 뜻. 화면 문구는 여기 없다 |
| `docs/copy.md` | **화면 문구를 쓰거나 고칠 때.** 말투와 내용 (구조는 `i18n.md`) |
| `docs/error-codes.md` | 에러 코드 레퍼런스 (출처는 `errors.py`) |
| `docs/privacy.md` | 로깅·개인정보 정책 |
| `README.md` | 저장소를 처음 세팅할 때. 요구 버전과 실행 명령 |

---

## 6. 작업 시 자문할 것

- **"이게 서버에 상태를 남기는가?"** 남긴다면 설계가 잘못됐다. (§1.1)
- **"이 문자열이 백엔드에서 오는가?"** 온다면 코드로 바꿔라. (§1.4)
- **"이 값이 CLAUDE.md에도 적혀 있는가?"** 그렇다면 문서에서 지워라. 코드가 출처다.
- **잘 모르겠으면 추측해서 구현하지 마라.** `docs/open-decisions.md`에 항목을 추가하고 물어라.

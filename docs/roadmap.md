# 범위와 로드맵

기능을 만들기 전에 "이게 지금 범위인가"를 여기서 확인한다.

## 지금 어디인가 (2026-09-03)

> **감사 국면을 R36에서 닫았다** (2026-09-21, 코드 소유자). 계획은 여섯
> 라운드(R35~R40)였고 **둘만 돌았다** — 목적이 배포 판정이 아니라 **부채 지도**였으므로
> 남은 넷은 지도에 자리만 남기고 접는다. 계획은 `docs/audit/plan-R35-R40.md`, 원자료는
> `docs/audit/map-2026-09-21.md`이고 **R37~R40의 슬라이스와 방법은 거기 그대로 있다** —
> 다시 열 때 요청서부터 쓰면 된다.
>
> **지적은 전부 닫고 닫았다.** 마지막까지 열려 있던 둘(지도 §3의 `R36-V V-1`·`V-2`)도
> 배포 전에 넣었다 — 따로 올린 테스트 표의 **타깃 열**을 정본과 같은 잣대로 보고(새 어휘
> 없이 `TARGET_NOT_NUMERIC`이다), 오류 문구에 실리는 셀 값을 자른다.

**지금은 기능 국면이다.**

V7까지 닫혀 사이트가 나갔고, 그 뒤로 **라운드 열**이 돌았다 — V11(R1~R5)이 소스를,
R6이 바로 그 전 세션의 diff를, R7~R9가 **`tests/` 자체**를 봤다. 지적은 전부
닫혔고 검사는 1,817개에서 **2,040개**가 됐다.

**국면을 닫은 근거는 A급의 성질이 세 번 옮겨갔다는 것이다** — V11은 **학생이
지금 다치는** 결함 일곱을 잡았고, R6은 그 세션이 **방금 만든** 결함 하나를,
R7~R9는 소스가 옳은데 **검사가 없던** 자리만 잡았다. 돌연변이 생존율은 내내
20~35%로 별로 안 변했다 — **감사는 0에 안 수렴한다.** 검사의 구멍은 그 코드를
만질 때 비용을 낸다 — 그러니 다음 감사는 **기능을 한 덩어리 끝낸 뒤의 diff 감사**다.
(R6이 그 모양이었고 가장 값이 높았다.)

**감사는 그만둔 것이 아니라 주기의 한 국면이다** — `기능 개발 → 감사`를 오가고,
**여는 시점은 사용자가 정한다.** 실제로 그 뒤 2026-08-29 ~ 31에 R12 · R13과 **스펙
감사**(사각 · 검증)가 돌았고, 결과는 위 문단의 판단을 뒤집지 않았다 — 잡힌 것이 대부분
**검사가 없던 자리**였다.

**기능 국면에 만드는 것은 다음 감사가 올 때까지 그물이 없다** — 관문이 유일한 층이다.
무엇이 달라지고 감사 국면이 어떻게 도는지는 `workflow.md` §3의 "감사가 안 도는 동안"과
"감사 국면은 어떻게 도는가"에 적었다.

**재야 할 것 셋** (2026-09-01, V12를 닫으면서 옮겨 왔다).

**코드는 다 있고 사람이 브라우저에서 몇 시간을 태우는 일만 남았다.** V12에 두면 그 절이
영영 안 닫히는 칸이 되고, 그 절의 입회 조건(*"결정은 섰는데 코드가 없는 것"*)이 뜻을
잃는다. **여기 있어야 표 한 번으로 보인다.**

| 무엇 | 돌릴 것 | 없으면 |
|---|---|---|
| ~~랜덤포레스트 상한~~ | ~~`tools/bench.html` [상한 찾기]~~ | **2026-09-10에 닫혔다** |
| ~~사진 칸 일곱 (`MLJS_IMAGE_*_ROW_LIMIT`)~~ | ~~같은 단추~~ | **2026-09-10에 닫혔다** |
| 사진 기준표 (`baseline.image`) | [전부 훑기] | **넷이 남았다** — 아래 |

**상한은 전부 닫혔다** (2026-09-10). 하니스를 **파일 하나로 구워** 다른 기계에서
7.08시간을 태웠고(`scripts/bench-standalone.mjs`), **아무것도 안 깨졌다** — 사다리 일곱이
끝점까지 완주했다. 그래서 알고리즘별 상한이 다섯 칸에서 사라지고(전역 상한과 같아졌다)
둘만 "재 본 곳까지"로 남았다. 원자료와 근거는 `open-decisions.md` "그러면 상한은 시간으로
정하는 것이 아니다"의 **넷째 실측**이다.

**사진 기준표는 셋만 찼다.** 상한 사다리가 기준표를 겸해서 의사결정트리·랜덤포레스트·SVM이
같은 판에서 나왔다. **남은 넷은 나이브베이즈·로지스틱·KNN·K-평균**이고, 그건 [전부 훑기]
쪽 사다리라 아직이다. **개발 PC 크롬에서 재야 한다** — 다른 기계의 절대값은 안 옮겨진다
(같은 결정문의 "다른 기계로 옮겨 봤다").

**남은 하나는 지금 학생이 다치는 자리가 아니다.** 사진 예상이 없다는 것은 **앱이 스스로
말한다**(2026-09-01). 그래서 급하지 않고, **그래서 안 보이면 잊힌다.**

~~**넷째 — 교정 배수의 대조**~~ — **2026-09-02에 쟀고 닫혔다.** 개발 PC에서
**110 · 144 · 166 · 264 ms**, Firefox **141 ms**, 하니스 **96~104 ms**. **계통 차이가
아니라 부하 잡음**이고, `CALIBRATION_BASELINE_MS = 100`은 그대로 둔다. 상세와 그때
접은 처방은 `open-decisions.md` "학습 예상 시간은 실측표에 기기 배수를 곱해 낸다"에 있다.

**대신 안 잰 것 하나가 남았다** — `onMounted`가 재기에 최악의 순간인가. 같은 결정문의
미결정 문단이 갖는다.

**감사가 안 본 채로 남은 것 둘을 적어 둔다.** 빚이 아니라 경계다 —
거기를 만질 때 먼저 볼 자리라는 뜻이다.

- ~~**스펙 24개를 아무도 한 줄도 안 열었다**~~ — **이 문장이 낡아 있었다** (2026-09-02에
  확인). "24"는 R9(2026-08-19) 시점의 수인데 명단은 어디에도 없고, 그 뒤 R12·R13의
  스펙 감사가 이 덩어리를 지나갔다 — 표본 다섯을 열어 보니(`columns.spec.ts` 727줄
  전량 · `thumbnails` · `toasts` · `router` · `app-*` 일부) **다섯 다 감사 지적에서 태어난
  회귀 검사를 품고 있었고**(R12 C-5 · R14-5 A-2 · V11 R5 B-1 · R14-5 A-7), 머리글
  주장과 단언이 일치했다. 돌연변이 둘(알림 자동소멸에 danger 추가 · 썸네일 unmount
  정리 제거)도 정확한 검사 하나씩이 물었다. **다섯 밖은 안 열었다** — 남는 조언은
  "그쪽 화면을 고칠 때 스펙을 먼저 열어라"이지, 안 열린 24개가 있다는 것이 아니다.
  진짜 남은 사각은 이 문단 아래 두 축과 `rule-coverage.md`의 지도다.
- **열여덟 라운드가 겨냥한 적 없는 축이 둘이다** (2026-09-02에 라운드 분포로 추린 것).
  V11 R1~R5는 살아 있는 결함을, R6·R10·R11·R17·R18은 diff를, R7~R9·R12·R13은 검사
  자체를 봤는데 — **판정 방법이 열여덟 라운드 내내 하나였다: 단일 파일의 한 줄을 바꿔
  단위 검사가 우는가(돌연변이).** 그 방법이 재현하지 못하는 두 자리는 어떤 요청서도
  축으로 세운 적이 없다.
  - **두 시점을 견줘야 잡히는 것 — 갱신 누락과 상태 이력.** 돌연변이는 한 시점의 계산을
    겨냥한다. *"수정한 날짜가 안 움직인다"*(V11 R5 A-1)나 *"이 문장이 페이지를 넘긴
    뒤에도 참인가"*(R5 B-8)는 값이 틀린 것이 아니라 **다시 왔을 때 안 바뀐 것**이라,
    `rule-coverage.md` "검사가 구조적으로 못 잡는 것"이 이름은 붙였어도 **감사가 겨눈
    적은 없다.** A급 둘이 우연히 여기서 나왔을 뿐이다.
  - **비결정적 실행에서만 나는 경합.** vitest는 결정적 단일 스레드라 async 두 개가
    겹치는 창을 저절로 만들지 않는다 — `limits-switch`의 *"읽는 중에 고른 선택이
    이긴다"*는 저장소를 세워 순서를 강제하고서야 물렸고, 그것도 A급을 고치다 부수로
    나온 것이지 요청서의 겨냥점이 아니었다. `ImagePredictPanel`의 `photosLocked`가
    막는 경합(예측 중 삭제가 저장 스냅샷의 사진을 되살린다)은 주석이 정확한데 무는
    검사가 없었다 — **R20이 순서를 강제해 재현했고 이제 `image-predict-race.spec.ts`가
    문다.** **경합을 축으로 세우려면 요청서가 "순서를 강제해 재현하라"를 명시해야
    한다** — 안 그러면 다음 라운드도 못 본다.
  - (추측이 아니다 — 근거는 라운드들의 지적 분포다. 다만 *"이 둘만이 유일한 사각"*은
    보증 못 한다. 실기기·실측·실제 스크린 리더는 이미 `rule-coverage.md`가 감사 밖으로
    적어 두었고, 여기 둘은 그와 달리 **감사가 볼 수 있었는데 방법이 못 본** 자리다.)
- ~~**그 사각이 실물을 하나 숨기고 있었다 — 두 탭 잠금이 결정만 서고 코드가 없다**~~ —
  **2026-09-04에 세웠다** (위 표). 아래는 그것을 찾아낸 경위이고, **사각 자체는 그대로
  남는다** — 없는 코드는 여전히 감사가 못 본다.
  (2026-09-02에 확인했던 것.) `open-decisions.md` "프로젝트는 한 번에 하나만 연다"(2026-08-05)가
  *"두 번째 탭에는 '이미 다른 탭에서 열려 있습니다'를 보여주고 편집을 열지 않는다"*까지
  적어 두었는데, `src/`에 `Web Locks`도 `BroadcastChannel`도 그 문구도 **0건이다.**
  결정문 자신이 적은 피해가 이 저장소의 "최악" 종류다 — 잊힌 탭 B의 저장 한 번이
  탭 A의 실험들을 **흔적 없이 덮는다.** 위 절의 규칙(*"결정문이 있고 구현이 없는 것은
  단계로 세워 이 표에 올린다"*)이 2026-08-29에 서기 전의 결정이라 목록에 안 잡혔고,
  **감사 열여덟 라운드는 존재하는 코드와 검사만 겨냥하므로 없는 코드를 볼 수 없었다.**
  경합 축이라 검사로도 못 잡는 자리다 — 코드 소유자가 우선순위를 정할 몫이다.
- ~~**`generate_sklearn_fixtures.py --check`가 `npm run ci` 밖에 있다**~~ —
  **2026-08-28에 관문 안으로 들어갔다.** 파이썬이 필요해 일부러 밖에 두었는데, 그러면
  사람이 손으로 돌리기 전에는 아무도 안 본다. **감사가 멈춘 것이 결정적이었다** —
  관문이 유일한 층인데 **조용히 틀린 숫자**를 잡던 층만 그 밖에 있었다.
  치르는 값은 관문(과 배포)이 `uv`에 매달리는 것이고, 결정문은
  `open-decisions.md` "sklearn 대조 픽스처가 CI 관문에 들어간다"에 있다.

**감사가 닫힌 뒤 기능 넷이 나갔다** (`0.10.3`~`0.10.6`) — 문서를 허브와 스포크로 나누고
(참조 1,400개를 `doc-refs.spec.ts`가 지킨다), 미결정 절에서 이미 닫힌 열셋을 걷어내고,
**사진이 들어갈 자리를 굽기 전에 묻게 하고**(배포 뒤 목록의 맨 앞이었다), MB를 십진으로
맞췄다.

**그 뒤로 `0.20.0`까지 왔다.** 표에 이름이 없으면 안 보이므로 여기 적는다 — 이 절이
`2026-08-20`에 멈춰 있던 동안 아래가 나갔고, 그중 둘은 **실물 `.mlpx`가 꺼낸 것**이다.

| 나간 것 | 무엇 |
|---|---|
| `0.11.0` | 첫 화면에서 예시 데이터셋으로 나가는 길 |
| `0.11.1`~`0.11.2` | R14와 스펙 감사(사각 · 검증)의 지적을 닫았다 |
| `0.11.3` | **수렴 경고가 막다른 길로 보내던 것** — `maxIter`를 늘리라고만 말했는데 그 데이터에서는 답이 스케일링이었다 |
| `0.12.1` | **모델이 배운 값**(계수 · 범주별 평균과 분산)을 결과 화면이 보여준다. 로지스틱 계수가 sklearn 대조 밑에 들어왔다 |
| `0.13.0` | **학습 예상 시간** — 누르기 전에 몇 초인지 말한다. 그리고 전처리의 정제 셋마다 설명 팝오버, 유형을 바꿔도 안 지워지는 층화 |
| `0.13.1` | 값 종류가 수천 개인 열이 전처리 미리보기를 멈추게 하던 것 |
| `0.14.0` | **실루엣 계수가 표본으로 나온다** — 10만 행에서 6분 반이던 지표에 2초 천장이 생겼다. 첫 화면에 저장소 링크, 열이 빠진다는 말을 주의 색으로 |
| `0.14.1`~`0.14.2` | 기준표 여덟을 평가까지 넣어 다시 세우고, **학습이 끝날 때마다 그 알고리즘의 배수를 다듬는다** |
| `0.14.3` | **상한을 시간이 아니라 실패로 정한다** — 재 보니 SVM 3,000→8,000 · KNN 10,000→50,000 · 트리 20,000→50,000까지 돌았다 |
| `0.15.0` | **학습 중에 경과 시간이 흐른다** — 화면에 움직이는 것이 없어 오래 걸리는 학습과 멈춘 탭이 같은 화면이었다. 예상 시간도 학습 중에 남는다. **실패한 학습이 기기 배수를 갱신하던 것**과 **층화가 성립 못 하는 몫을 조용히 지나가던 것**(sklearn이 던지는 자리)을 함께 고쳤다 |
| `0.16.0` | **인공신경망(다층 퍼셉트론)이 들어왔다** — 태그 이름이 이것이다(릴리스 커밋 뒤·태그 앞에 들어왔다). 표·사진, 분류·회귀 전부 순수 JS이고, 결정문은 `open-decisions.md` "인공신경망을 넣는다 — 손잡이는 층 수와 뉴런 수 둘". 그리고 **사진을 붙여넣어 올린다**(Ctrl+V) — 데이터 화면은 범주 없음으로 받고 예측 화면은 새 데이터로 받는다. 붙여넣기는 **덧붙는다**(드롭과 갈리는 것은 규칙이 아니라 되돌릴 수 있는가다). **분류를 고르면 학습 화면에서 쫓겨나던 것**과 **화면에 사진이 있는데 "데이터가 0개"라고 말하던 것**을 함께 고쳤다. 감사 R19~R24를 전부 닫았고 검사가 2,738개에서 2,949개가 됐다 |
| `0.17.0` | **고르는 자리는 잠그지 않는다** — 사진 프로젝트에서 군집을 고르면 분류 카드가 잠기는데 그 잠금을 풀 방법이 화면에서 사라져 있었다(교실 보고). 못 하는 조합은 [학습하기]가 사유와 함께 세운다. **갈라 볼 것이 없는 학습에 경고를 붙였다** — 값이 한 종류인 타깃은 분류에서 정확도 100%, 회귀에서 결정계수 1.000을 내고 교실에서 만점은 성공으로 읽힌다. **선형회귀를 센터링 + SVD로 바꿨다** — 정규방정식이 척도가 갈린 표에서 계수의 부호까지 틀리고 있었다(참값 3986.58을 −120.68로). 감사 R25를 닫았고 검사가 2,949개에서 3,026개가 됐다 |
| `0.18.0` | **학습을 코어로 가른다 — 결과는 코어 수와 무관하다** — 신경망·랜덤포레스트·KNN이 워커로 갈라 돈다. 실물 크롬에서 **포레스트 1.95배 · KNN 2.36배 · 신경망 1.76배**이고 **셋 다 직렬과 결과가 같다**(신경망은 모델 JSON이 바이트 단위로). 결정성이 우연이 아니라 구조다 — 고정 조각 접기 · 씨앗 사슬 재생 · 행 독립. 문턱 아래거나 워커를 못 띄우면 **직렬로 물러난다.** 그리고 **프로젝트는 한 번에 하나만 연다** — 잊힌 탭의 자동 저장이 이쪽 실험을 흔적 없이 덮던 자리다. 감사 R26을 닫았고(A 5 · B 11 · C 7) 검사가 3,026개에서 3,076개가 됐다 |
| `0.18.1` | **잠금 밖에서 저장소를 쓰지 않는다** — 두 탭 잠금이 편집 화면에만 걸려 있어 목록 화면의 두 동작이 잠금을 **묻지도 않고** 저장소를 썼다. `.mlpx` 가져오기는 같은 `projectId`로 **덮어쓴 뒤에** 거절이 왔고(다른 탭의 실험이 그 사이에 사라진다), 삭제는 다른 탭이 편집 중인 것을 지웠다. 그리고 **잠금 교체가 실패해도 앞의 것이 이미 놓여** 있어, 거절당한 탭이 자기 화면의 프로젝트를 쥔 자물쇠 없이 자동 저장하고 있었다. 셋의 뿌리가 하나라 원시 연산 둘을 세웠다. 감사 R27을 닫았고(A 2 · B 3) 검사가 3,076개에서 3,082개가 됐다 |
| `0.18.2` | **값을 구분하는 색을 거리순으로 고른다**(#18) — 셔플이 두 색이 얼마나 벌어졌는지를 안 봐서, 두 갈래 분류의 카드 둘이 ΔE2000으로 **2.4**까지 붙었다(교실 보고). 첫 색만 무작위이고 그다음부터는 거리가 정한다 — 카드 2갈래 최악이 **2.4 → 11.9**, 표·산점도 4갈래가 **9.3 → 26.7**. 자리는 셋이었지만 없는 원시 연산 하나였다. **그리고 중단한 실험이 설정을 바꿨다고 말하던 것**(#20) — 학습을 멈추면 뒤쪽 모델은 run이 안 남는데 하이퍼파라미터를 run에서 읽어 "있다가 없어졌다"로 잡혔다. 아무것도 안 바꾼 학생에게 *"알 수 없는 설정이 바뀌었습니다"*가 다섯 줄 떴다. **이미 나간 파일도 조용해진다** — 파일은 안 고치고 그리지 않는다. 검사가 3,082개에서 3,112개가 됐다 |
| `0.19.0` | **안 깨지는 곳까지 상한을 올렸다** — 하니스를 **파일 하나로 구워** 다른 기계에서 7.08시간을 태웠고 **아무것도 안 깨졌다**(사다리 일곱이 끝점까지 완주). 그래서 KNN·의사결정트리가 **100,000행**, SVM **20,000행**, 랜덤포레스트 **50,000행**, 사진 셋이 **5,000장**이 됐다 — 옛 상한은 전부 *시간*을 근거로 잡은 값이었고, 그 근거는 2026-09-01에 이미 상한의 것이 아니게 됐다. **교실만이 학습이 도는 곳이 아니다.** 그리고 **사진 기준표 셋을 함께 채웠다** — 상한만 올리면 사진 랜덤포레스트에서 2.3시간짜리를 아무 경고 없이 시작하게 된다 |
| `0.20.0` | **신경망 예상 시간이 10,000행 위에서 열렸다** — 기준표가 20,000행에서 막혀 있었다. 라벨을 특성과 무관하게 주는 것만으로는 은닉 100개가 2만 행의 잡음을 못 외워 **31 에폭**에 멈췄고, 그래서 20,000행이 10,000행보다 빨랐다. `NeuralOptions.tol`을 열어(sklearn `MLPClassifier`의 같은 이름, 등록부에는 안 올린다) 하니스가 `tol: 0`으로 **천장**을 잰다 — **20,000행 31.6초 · 50,000행 88.7초**, 여섯 점 전부 200 에폭이다. 옛 표가 50,000행을 102.7초로 말하던 자리다. **첫 실측 판은 버렸다** — 발열로 뒤로 갈수록 부풀어(5,000이 3.36배) 사다리가 10,000에서 섰고, 합격 기준을 *"사다리가 20,000까지 스스로 가는가"*로 적어 뒀다. 틀린 단정 넷(*"여기는 언제나 천장을 지난다"* 외)을 고치고 검사가 3,114개에서 3,119개가 됐다 |
| `0.21.0` | **교사가 제출물을 열어 보는 화면이 섰다** — 폴더째 고르면 명렬이 표로 서고, 한 줄을 고르면 결과 화면의 부품 그대로 열람·무결성·재실행 대조가 붙는다. 원본 `.mlpx`는 어느 경로로도 안 고친다. 같은 `projectId`를 가진 파일을 묶어 보여 주고(**판정이 아니라 사실이다** — 교사가 나눠 준 시작 파일이면 반 전체가 같다), 서른 명의 글을 한 묶음으로 내려받는다. **감사(R28)가 A 둘·B 아홉·C 스물셋을 꺼냈고 전부 닫았다** — 뿌리는 *"규칙의 이름이 그물보다 넓다"* 하나였다. `ui-rules`의 "워커를 여는 화면"이 사진 워커만 훑어 대조 판이 통째로 면제였고, `retire`가 없어 `alive()` 가드 셋이 **죽은 채로 초록**이었다. 표 정렬 훑기는 열이 어긋나도 조용했고, 묶음을 굽는 동안 줄을 누르면 화면이 잠겼다 |
| `0.21.1` | **재검증 감사(R28-V)가 꺼낸 것을 닫았다.** 회귀는 0건이었고 새 결함이 하나 — 묶음을 굽는 동안 `entries`가 **서른 개의 통째 파싱 결과를 동시에 들었다**(정본 표·모델·사진까지). zip에 나가는 것은 글과 첨부뿐이라 **타입을 그 둘로 좁혀** 막는다. 그리고 떠날 때 명렬도 멈춘다 — 화면의 `retire`는 워커까지이고 파일 읽기 루프에는 맡길 손잡이가 없어서, 서른 개를 훑다 나가도 큐가 끝까지 돌았다. **멈춘 자리가 몇 중 몇이었는지도 남긴다**(`넷 중 셋`과 `셋 중 셋`이 같은 모양이었다). 나머지 여덟은 전부 *"코드는 옳은데 검사가 없다"*였고 — 손이 여럿일 때, 절반을 읽은 뒤 갈릴 때, 판정이 두 길로 올 때 — 그 자리마다 검사를 세웠다. **그물의 기준도 이름에서 일로 옮겼다**: `alive()`를 묻는가가 아니라 `useWork()`를 부르는가다. 검사가 3,347개에서 3,358개가 됐다 |
| `0.21.2` | **최종 검토(R28-F)가 남긴 그물 둘을 조인다.** 떠나는 자리에서 `retire`를 **낱말**로 찾고 있어 `void retire`처럼 언급만 해도 통과했고 — 이 파일은 같은 병을 한 번 앓았다(`import` 줄이 등록으로 세어지던 자리) — 화면이 떠날 때 명렬을 멈추는지는 아무도 안 봤다. 그리고 **브라우저로 실측했다**: 대조를 200ms에 멈추면 `멈춤 (4/5)`가 남고 안 돌린 다섯째에 `엔진 없음`이 안 선다, 다시 누르면 그 줄이 사라진다, 묶음 zip은 네 명 몫이 1,092바이트다(파일 하나가 42KB인데 — 글과 첨부만 든다). 1024px에서 표 넘침 0 |
| `0.21.3` | **의존성을 한 바퀴 올리다 관문이 세 번 빨개졌고 셋 다 진짜였다.** ① zod 4.6이 `z.iso.datetime()`에서 **초를 필수로** 바꿔 `2026-09-18T10:30Z`를 거부한다 — 우리가 쓴 파일은 안 걸리지만 **`.mlpx`가 받는 집합을 라이브러리의 내부 정규식이 정하고 있었다.** 그날의 정규식을 우리 코드로 옮겨 소유했고(`.meta({ format })`로 JSON Schema 출력까지 같게 둬서 정본도 `FORMAT_VERSION`도 안 움직였다), 무엇을 받고 거부하는지를 표본으로 못 박았다. ② npm이 트리를 재배치해 `@vue/*`가 `vue/node_modules/` 아래로 내려가자 **`vue` 패키지 자체 파일이 번들에서 사라져** 고지의 대표가 없는 이름을 기다렸다 — 실제로 실리는 넷을 세어 보고 `@vue/runtime-core`로 바꿨다. ③ jsdom 30.1이 vitest 4의 shim과 안 맞아 **검사는 전부 통과하는데 관문이 빨갛다** — `~30.0.1`로 묶고 사유를 양쪽에 적었다(푸는 조건은 vitest 5). 프런트 17개·백엔드 12개를 올렸고 dependabot은 **달 1회**로 늦췄다(머지된 PR이 하나도 없었다). 그리고 **손댄 파일을 열 때 뜨는 경고가 뜨자마자 사라지던 것**을 고쳤다(사용자 지적) — 어조가 아니라 자리였다. 라우터가 걷는 수위선 아래에 들어서였고, **걷는 쪽은 어조를 안 본다.** 검사가 3,359개에서 3,367개가 됐다 |
| `0.21.4` | **vitest 5.** 검사 3,364개가 그대로 통과했다 — 미리 훑은 깨는 변경 여섯 중 걸린 것이 없었고 설정 넷도 그대로 선다. **`dist/`는 한 바이트도 안 바뀐다**(자산 열넷이 라이브와 같은 해시다) — 검사 도구만 움직였다. 그리고 **어제 적은 해제 조건이 틀렸던 것을 고쳤다**: *"vitest 5를 받으면 jsdom 고정이 풀린다"*고 세 군데에 적었는데 vitest 5도 같은 shim을 그대로 갖고 있었다(`makeCompatBlob`이 여전히 jsdom 내부 `Blob._buffer`를 읽는다). 재 보지 않고 버전 경계를 원인으로 지목한 것이다. **실제로 푼 것은 우리 쪽 관용구였다** — 이 저장소의 검사는 객체 URL을 `<img src>`의 글자로만 보지 되읽지 않아 가짜 글자로 충분하고, 사진 판 일곱이 이미 그렇게 하고 있었다. 그 자리에 이름을 붙여 빠져 있던 여덟째를 채우니 jsdom을 묶어 둘 이유가 사라졌다 |
| `0.22.0` | **scikit-learn이 브라우저에서 돈다** — Pyodide를 원본에서 받아(27.3MB · 시동 약 8.7초) **여덟 알고리즘 전부** 학습·예측·저장이 선다. 결정문은 `open-decisions.md` "scikit-learn(Pyodide)은 원본에서 받고, 시동은 학습마다 낸다". **잠그지 않고 고르기 전에 비용을 말한다.** 랜덤 포레스트가 마지막이었다 — v1으로 담으면 예측 규칙이 달라(확률 평균 대 다수결) **387행 중 12행이 갈려** 한때 안 담았는데, *"학습해 놓고 예측하지 못하면 그 run은 수행평가에서 반쪽"*이라 **잎이 분포를 드는 `mlpx-tree-v2`**를 세웠다(`mlpx-spec.md` §5.3.1). **마이그레이션은 안 붙는다** — 형식 이름은 어휘 지문의 항목이 아니라 자기 자신이 버전이다. 파일에는 **어느 배포판으로 만들었는지**가 적히고(`run.engine.version` + `packages`) 재실행이 그 판을 받아 돈다. **예상 시간도 실행 방법마다 다른 수를 말한다** — 두 엔진은 기준표도 손잡이 배수도 시동도 갈려서(사다리 열하나를 새로 쟀다: 그루 수 0.69배 · 반복 횟수 평평 · 특성 여덟 다 `flat`) 순수 JS 표를 빌리면 **열여섯 배 틀린다.** **큰 모델은 만들기 전에 거절한다** — 기본 100그루 × 100,000행이면 113MB이고 옮기는 동안 힙이 정점 479MB라, 파이썬이 배열을 꺼내기 전에 **JSON 문법이 정하는 하한**으로 세어 막는다. 감사 **R29~R34**를 돌려 전부 닫았다(A 5 · B 6 · C 30). 뿌리는 둘이었다 — **`?? 기본값`이 타입을 통과하는 침묵**(같은 필드에서 두 라운드 연속)과 **세는 것이 실물이 아니라 표기였던 것**(문 목록을 정규식으로, 속성 자리를 깊이로). **못 여는 문서를 만드는 길도 문에서 막았다**: 쓰는 길에는 검증이 없고 읽는 길에만 있어 한 번 담기면 파일도 저장소 사본도 **저장은 성공하고 다시는 안 열렸다**(원문 235자 · `nSamples: 1`). 이제 문 전부를 검사가 훑고 **목록이 닫혀 있는지도 센다.** 그리고 **예측 입력의 글자와 천 단위 쉼표가 조용히 `0`이 되던 것**을 고쳤다 — `1,650`이 `[0, …]`이 되고 실패도 경고도 없이 답이 나왔다. 검사가 3,367개에서 3,575개가 됐다 |
| `0.23.0` | **채점하는 행렬이 조용히 `0`이 되던 길을 막고, 천 단위 쉼표를 정본에서 푼다.** 감사 **R35·R36**을 돌렸다(A 1 · B 8 · C 12). 뿌리가 둘이었다 — R35는 ***"열린다"를 "맞다"로 읽은 것***(sklearn 어댑터의 문 일곱이 값을 안 재고, 처방으로 세운 첫 검사까지 *파일이 열리는가*만 봐서 다섯 중 셋만 물었다), R36은 ***그물이 값이 들어가는 자리에만 있고 축이 넓어지는 것에는 없는 것***이다(알고리즘 등록부의 과제 유형 축이 아홉 중 여섯 조용). **A는 하나였고 학생이 지금 다치는 것이었다** — 열 판정은 훈련 몫만 보는데 `transform`은 시험 몫도 돌려서, 시험 몫에만 있는 `1,650`·`없음`이 **조용히 `0`이 되어 정확도와 R²에 섞였다.** 실패도 경고도 없이 그 숫자가 포트폴리오에 적혔고, 300행에서 회귀 R²가 **1.000에서 −54.0**이었다. `planRun`이 `FEATURE_NOT_NUMBER`로 학습 전에 막는다. **그리고 그 고침을 다시 감사해 둘을 더 고쳤다** — 내가 쓴 주석이 *안 무는* 스펙을 무는 검사로 지목했고(R36의 뿌리인 *유창하게 틀린 주석*이 그 고침 안에서 재발했다), 결정문이 재실행 경로에 이 문이 걸린다는 사실을 안 적었다. **천 단위 쉼표는 읽는 자리에서 푼다** — 파이썬에서도 `thousands=','`는 `read_csv`의 인자이지 `fit`의 인자가 아니라, 설정이 아니라 `importTable`에 두어 **`formatVersion`이 안 움직이고 마이그레이션도 없다**(옛 `.mlpx`는 저장된 바이트를 그대로 읽는다). **쉼표를 지우는 것이 아니라 묶음을 알아본다** — `1,65`는 유럽식 소수점일 수 있어 안 건드린다. 엑셀이 쉼표를 남기는 조건은 **셀 서식이 텍스트일 때** 하나이고 실물로 확인했다. 검사가 3,575개에서 3,624개가 됐다 |

| `0.24.0` | **데이터 화면에서 열을 골라 그림으로 본다** — 히스토그램 · 막대그래프 · 박스 플롯 · 산점도 넷. 결정문은 `open-decisions.md` "44. 데이터 화면의 시각화", 화면의 모양은 `architecture.md` §8.9.1. Orange3의 Visualize 위젯 스무 개 중 **넷만 담았다** — 투영 계열은 축을 설명하는 비용이 그림값을 넘고, Tree Viewer 계열은 모델의 결과물이라 데이터 화면의 물건이 아니다. **의존성을 한 줄도 안 늘렸다**: 박스 플롯은 `[Q1, Q3]` 떠 있는 막대이고 수염·중앙값·이상치는 우리가 캔버스에 직접 그린다(전국 × 리셋되는 교실 PC에서 플러그인 하나가 차시마다 다시 내려간다). **열이 먼저고 도구가 나중이다** — 여는 손잡이는 열 요약의 줄이고, 못 쓰는 도구는 숨기지 않고 **등록부의 gate가 준 이유와 함께** 잠긴다. 구간 수는 numpy `histogram_bin_edges`의 `'auto'`, 수염은 튜키의 1.5배 — **우리가 고른 수가 아니라 바깥의 규약이라 검사도 그 규약을 다시 계산해서 본다.** **그리고 이 기능이 이 저장소의 그물에 구멍 하나를 드러냈다** — 검사 3,705개가 초록인 채로 **결함 열이 살아 있었고 사람이 브라우저로 몰자 전부 나왔다**: 창이 한 번도 안 열렸고(`v-if`로 태어나 감시자가 안 깨어남), 히스토그램의 막대가 안 갈렸고(테두리를 막대와 같은 색으로 그음 — **주석은 바탕색이라 적혀 있었다**), 박스 플롯의 축이 0에서 시작했고, 두 번째 상자에 수염이 없었고(플러그인이 재료를 닫힘으로 들었다), **닫힌 `<dialog>` 열 개가 화면에 눌러앉았고**(`display: flex`가 브라우저의 숨김을 덮었다), **`sr-only` 넷이 문서를 늘렸다**(담는 상자가 없어 화면 기준으로 서서 `body`는 그대로인데 `html`만 늘었다 — 상태 표시줄 아래 빈 칸의 정체가 이것이었다). **캔버스와 `<dialog>`의 열림은 jsdom에 없어서 검사가 구조적으로 못 본다** — 그래서 이 기능의 합격 조건에 *사람이 한 번 몰아 본다*가 들어갔다. 못 본 자리를 메우는 그물 셋을 세웠다(등록부의 잠금 · 눈으로만 보이는 그림 규칙 · `sr-only`의 담는 상자). 검사가 3,624개에서 3,743개가 됐다 |

| `0.25.0` | **데이터 화면이 한 열로 펴지고, 두 열은 정말 넓은 화면에서만 갈린다.** `md`(768px)에서 갈랐는데 **그 폭에서는 두 판이 다 잘렸다** — 12열짜리 표로 재니 요약은 약 581px이 하한인데 275px을 받았고(결측치를 보라고 만든 판이 결측치 칸을 못 보여줬다), 미리보기는 1366에서 12열 중 일곱만 보였다. **§8.9가 두 열을 정당화한 근거가 좁은 폭에서 스스로 무너져 있었다** — *"요약에서 본 열을 표에서 같이 본다"*인데 그 열이 표에 안 보였다. 한 열로 펴니 같은 1366에서 표가 1,373px을 받아 12열이 전부 보인다. 순서는 요약 → 미리보기이고, 세로를 안 다투게 되어 **같은 부품을 두 번 그리던 `<details>` 복사본**과 한 번도 참이 된 적 없는 플래그가 사라졌다. 두 열은 `3xl`(1920px)에서만 선다 — 1536은 **1920 모니터를 125%로 쓰는 흔한 교실 PC의 폭**이라 거기서 가르면 사실상 전부가 된다. **`3xl`은 Tailwind에 없어서 우리가 세웠고**, 정의 안 하면 클래스가 안 만들어질 뿐 아무도 안 우는 자리라 그것을 무는 검사를 함께 걸었다. 학습 화면의 두 열도 `lg`로 한 단계 올렸다. 산점도의 점은 Chart.js 기본값 3을 그대로 쓰고 있었다(5로). 그리고 **배색 토큰을 읽는 자리를 하나로 합쳤다** — 셋이었고, 2026-08-29 감사가 잡은 결함이 그 모양이었는데 그때 고친 것은 복사본 하나뿐이었다. **폭은 검사가 못 본다**(jsdom에는 배치가 없다) — 375·768·900·1024·1366·1536·1920에서 사람이 쟀고 그 사실을 `rule-coverage.md`에 적었다 |
| `0.25.1` | **상한이 막으면 푸는 자리를 함께 말한다.** 10만 행 파일이 거절당하는데 문구가 `[상한 해제]`를 한 마디도 안 했다 — 학생은 **있는 길을 모른 채 데이터를 줄인다.** 붙인 곳은 여섯이고 기준은 둘이다: `limits-switch.ts`가 그 상한을 풀고, 그 상한이 학생의 다음 걸음을 막는다. **닮은 것들을 세어서 갈랐다** — 상한을 말하는 문구가 열넷인데 히스토그램 구간 수·모델 예산은 스위치가 안 풀고, 기기의 남은 공간과 서버 상한은 우리 것이 아니며, 산점도 표본은 막지 않고 줄이기만 한다. **목록 없이 "상한 문구에 다 붙이자"로 갔으면 여덟 자리에서 "누르면 풀린다"고 거짓말을 했다.** 런타임 코드는 0줄이다 — 해제된 상태에서는 이 여섯이 애초에 안 나므로(`Infinity` 비교) 스위치를 감시하는 코드는 죽은 코드였고, 처음 계획이 그것을 넣으려 했다. 경고도 안 적는다: 버튼이 팝오버 안이라 **누르려면 `limitsRisk`를 반드시 지나간다.** **위치(`화면 오른쪽 하단`)는 어떤 검사도 못 본다** — CSS 뒤이고 그 자리에 글자 없는 아이콘이 둘이라, 사람 확인임을 `copy.md` §5에 적었다 |
| `0.25.2` | **히스토그램의 구간 수를 학생이 정한다.** 결정문은 `open-decisions.md` "45. 히스토그램의 구간 수를 학생이 정한다", 화면 규약은 `architecture.md` §8.9.1.1. 10만 행짜리 치우친 열에서 막대 셋과 **빈 구간 190개**가 나왔다 — 그런데 **그림은 틀리지 않았다**: `numpy.histogram`이 같은 열에 더 잘게 쪼갠 같은 모양을 준다. **문제는 그림이 아니라 학생이 할 수 있는 일이 하나도 없다는 것이었다.** 처음에는 1.5×IQR 밖을 마지막 막대로 모으는 안을 권했다가 **재 보고 철회했다** — 이 데이터는 Q1도 1, Q3도 2라 울타리가 3.5이고 **꼬리가 아니라 본체가 잘린다.** 게다가 자동으로 x를 자르는 라이브러리가 없다(Excel도 matplotlib도 범위는 사람이 준다). 담은 것은 `numpy.histogram(a, bins=20)`의 `bins` 그대로다 — **우리가 지어낸 손잡이가 아니라 학생이 파이썬에서 칠 이름**이고, 파리티가 오히려 강해진다. **`maxBins`와 자리를 갈랐다**: 합쳤다면 학생이 고른 20이 자동 5짜리 열에서 조용히 5가 된다(검사 다섯이 그 실수를 문다). 수를 직접 주면 사분위 범위가 필요 없어 **정렬이 아예 안 일어난다.** 화면은 자동 체크박스(즉시) · `readonly` 칸(**`disabled`가 아니다 — 그 숫자는 학생이 파이썬에 옮겨 적을 값이다**) · [적용] 버튼(디바운스 대신) · 범위 밖이면 **잠그고 말한다**(조용히 당기면 2026-08-12 B-3이 되돌아온다) · 그림이 자기가 그린 구간 수를 말한다. **그리고 사람이 브라우저로 몰자 검사가 못 본 것 셋이 나왔다** — 오류 문구가 도움말과 **글자가 같아** 색만 붉어졌고, 스위치가 칸 위에 떠 있어 어느 칸의 것인지 알 수 없었고, [적용]이 좁은 화면에서 **굴려야 보였다.** 셋 다 §8.9.1.1에 규약으로 올렸다. **꼬리는 여전히 안 보인다** — 그것은 y축 로그의 몫이고 이번에 안 담았다 |
| `0.25.3` | **히스토그램의 세는 축을 로그로 볼 수 있다.** 결정문은 `open-decisions.md` "46. 히스토그램의 y축을 로그로 볼 것인가". 구간 수를 줄여도 y축이 최빈 구간에 잡혀 있는 한 1·2개짜리 막대는 0픽셀이라, **꼬리를 보이게 하는 수단은 이것 하나뿐이다.** **하루 만에 뒤집힌 결정이다** — 미룬 근거가 "설명 비용" 하나였고 대안으로 박스 플롯을 들었는데, 같은 열의 박스 플롯도 **상자가 축 높이의 1.4%**라 안 읽힌다(Q1=1·중앙값=1·Q3=2). 전제가 틀려서 미룰 자리가 없어졌고, **미룬 사정은 결정문에 지우지 않고 남겼다.** [적용]도 자동도 없다 — 축의 종류는 **다시 세는 일이 아니라 다시 그리는 일**이고, 축에는 고를 것이 없다. **그리고 브라우저로 몰자 검사 3,784개가 못 본 둘이 나왔다**: `Chart.register`에 `LogarithmicScale`을 안 넣어 **그림이 통째로 사라졌고**(설정은 옳고 화면은 빈 상태가 초록이었다 — 옵션은 `chart-config.spec`이 물지만 등록은 다른 파일의 일이라 잇는 검사가 없었다), 세는 축에 `0.1`·`50,000.0` 같은 **개수로는 있을 수 없는 눈금**이 섰다(`precision`은 선형 눈금의 것이다). 앞엣것은 `tests/chart-registry.spec.ts`가 그 이음매를 물게 했고 — **`<script setup>`의 최상위 코드는 import가 아니라 setup에서 돌아** 부품을 들이는 것만으로는 등록이 안 일어나므로 `ui-rules`처럼 소스를 읽는다 — 뒤엣것은 바닥을 1로 박고 정수만 세운다. **같은 날 낮은 막대의 호버도 고쳤다**: `intersect: true`라 커서가 막대 사각형 안에 있어야 반응했는데 치우친 열은 대부분의 막대가 **선**이라 못 짚었다. `axis: 'x'`로 세로 줄 어디서나 짚힌다(박스 플롯·산점도는 반대라 그대로 두고 셋을 한 검사에 갈라 못 박았다) |
| `0.26.0` | **시각화가 세 갈래로 넓어졌다.** ① **세는 축을 로그로 본다**(결정문 46) — 구간 수를 줄여도 y축이 최빈 구간에 잡혀 있는 한 꼬리는 0픽셀이라, 그것을 푸는 수단이 이것 하나다. **하루 만에 뒤집힌 결정이다**: 미룬 근거가 "설명 비용" 하나였고 대안으로 박스 플롯을 들었는데 **같은 열의 상자가 축 높이의 1.4%**라 안 읽혔다(Q1=1·중앙값=1·Q3=2). ② **산점도가 범주 열을 축으로 세운다** — 결과 화면의 군집 산점도가 이미 그렇게 그리는데 데이터 화면만 *"수치 열에서만"*이라, **같은 표의 같은 두 열이 화면마다 다르게 취급됐다.** 흩뿌림과 범주 눈금을 `data/category-axis.ts`로 **들어내서**(복사가 아니라 이동이라 군집 검사 서른이 그대로 통과했다) 양쪽이 쓰고, 범주 차례도 `preprocess`의 인코딩 순서(첫 등장)에 맞췄다. 잠금 규칙이 하나로 줄면서 `needsAnotherNumeric`이 거짓이 되어 **이름·두 언어 문구·주석·검사를 함께 고쳤다** — `doc-refs.spec.ts`가 옮긴 심볼을 가리키던 주석을 잡았다. ③ **점검 화면에 mlpx를 끌어다 놓는다** — 데이터 화면의 점선 과녁과 같은 모양이다. **점선은 꾸밈이 아니라 약속**이라 받는 손 없이 그리면 교사가 제출물을 놓고 아무 일도 안 일어나는 것을 본다. 폴더째는 단추로 남기고 문구도 그렇게만 말한다. **그리고 실물이 검사가 못 본 것을 또 꺼냈다**: 로그 축은 `Chart.register`에 눈금을 안 넣어 **그림이 통째로 사라졌는데**(설정은 옳고 화면은 빈 상태가 3,784개를 통과했다) `tests/chart-registry.spec.ts`가 그 이음매를 물게 했고, 세는 축에 `0.1`·`50,000.0`이 서던 것은 바닥을 1로 박고 정수만 세운다. **시각화 창도 손봤다** — 도구 단추가 넓은 화면에서 두 열(`@sm`, 문턱을 재서 잡았다), 낮은 막대도 세로 줄 어디서나 짚히고(`axis: 'x'`), 구간 수 칸이 단추와 같은 높이라 자동을 껐다 켤 때 줄이 안 튄다 |
| `0.26.1` | **색이 겹치면 범례를 안 세우고 겹친다고 말한다** (결정문 47). 경찰서 100여 개로 색 구분한 산점도에서 범례가 여덟 줄을 먹어 그림보다 컸는데, **자리보다 먼저인 문제가 있었다** — 팔레트가 일곱이라 같은 색을 열넷이 나눠 쓰고 있었고 **범례가 없는 대응을 있다고 말하고 있었다.** **처음 낸 안은 "갈래가 일곱을 넘으면 색 구분을 잠근다"였고 코드 소유자가 물려세웠다** — seaborn도 plotly도 matplotlib도 막지 않으니 **그것도 우리 발명**이고, 앞서 `기타`로 합치는 안을 *"어느 도구도 안 그런다"*로 접었으면서 같은 잣대를 막는 안에는 안 댄 것이 실수였다. 게다가 이 저장소가 잠그는 것은 **계산이 성립하지 않는 것**인데 이 그림은 그려지고 읽힌다(점을 가리키면 툴팁이 이름을 말한다). 그래서 **막지 않는다** — 점은 갈래가 몇이든 전부 그리고 범례만 안 세우며, 그림 아래 한 줄이 겹친다는 사실과 식별하는 방법을 말한다. 경계는 팔레트가 이미 정한 수라 **새 상수가 0개**다 |
| `0.26.2` | **눈으로만 보이는 것 넷을 고치고, 홈 화면에 얹는 앱으로 섰다.** ① **아이패드에서 확인창이 제목 높이로 잘렸다** — `AppDialog`의 속살이 `h-full`을 무조건 받아 `max-h`를 넘겨받지 못했고, 좁고 낮은 화면에서만 드러났다. 전수로 훑는 검사를 세웠다. ② **산점도가 보이지도 않는 획을 점마다 그었다** — 점 반지름이 5인데 테두리 1을 10만 번 그리고 있었다. `borderWidth: 0`과 `parsing: false`로 줄였고 **`normalized: true`는 안 넣었다**(우리 자료가 그 약속을 못 지킨다). 잰 값은 커밋에 있다. ③ **힙독을 훅이 막는다** — `CLAUDE.md`에 규칙으로 적어 둔 자리를 또 밟아 검사 파일이 파스조차 안 됐다(`\b`가 제어문자가 됐다). *"사람이 지키길 기대하지 않는다"*를 여기에도 적용했다. ④ **PWA 태그** — 파비콘이 **아예 없었고**(탭에 브라우저 기본 문서 아이콘이 떴다) 매니페스트·안전 영역·띠 색이 함께 들어갔다. 경로는 전부 상대다(`/`로 시작하면 Pages에서 **앱이 아니라 404가 설치된다**). 서비스 워커는 **안 넣었다** — 캐시 전략은 교실 PC가 차시마다 리셋된다는 전제 위에서 다시 따져야 하는 결정이라, 그 부재를 `pwa.spec.ts`가 못 박았다. 띠 색은 운영체제 설정만 보면 학생이 앱 안에서 고른 배색과 어긋나므로 `applyTheme`이 고쳐 쓰는데, **CSS 토큰을 읽지 않는다** — *"배색 토큰을 읽는 자리는 컴포저블 하나뿐"*(`0.25.0`)이 물었고, 검사의 기대값을 넓히는 대신 `index.html`이 첫 그림용으로 이미 들고 있는 두 색 중에서 고르게 했다. 그 사본이 토큰과 갈라지면 운다(심어서 확인). **아이패드의 홈 화면 아이콘은 안 쟀다** — iOS가 매니페스트의 SVG를 쓰는지 재지 않았으므로 PNG는 결정을 받고 넣는다. 그리고 **R37 감사의 A 셋**을 고쳤다(두 탭 잠금의 경합, `Object.hasOwn`) |
| `0.26.3` | **아이패드에서 내보낸 제출물을 점검 탭이 안 열었다.** 결정문은 `open-decisions.md` 48. 사파리가 `비올까.mlpx`를 **`비올까.mlpx.zip`으로 저장한다** — `Blob`의 종류가 말하는 확장자를 이름에 덧붙인다. **원인을 코드로는 못 찾았다**: 윈도우 개발 PC에는 웹 인스펙터가 없어 기기 콘솔을 볼 수 없었고, **임시 Vite 플러그인으로 기기 로그를 개발 서버 터미널로 끌어와서** 한 줄로 판정했다(`accept=".mlpx" count=1 {"name":"비올까.mlpx.zip"}`). `accept`는 막지 않았고 떨어뜨린 것은 `rosterOf`의 이름 판정 하나였다. **셋을 고쳤다.** ① 내보내는 종류를 `application/octet-stream`으로 — `application/zip`이 **틀린 값은 아니지만**(실체는 zip이다) 그 값이 이름을 더럽히고 그 이름이 **파일 앱의 압축 해제를 부른다.** 풀리면 제출물이 폴더로 흩어지고 서버가 없어 그 파일이 학생의 유일한 반출 경로다. 바꾼 뒤 아이패드에서 재니 `.zip`이 안 붙는다(**사람 확인** — 브라우저의 저장 동작은 검사가 못 본다). 포트폴리오 묶음은 **안 바꿨다**(교사가 실제로 푸는 zip이다). ② 읽는 쪽이 `.mlpx.zip`도 받는다 — 종류를 고쳐도 **이미 기기에 저장된 파일은 이름이 안 바뀐다.** 관용의 범위는 그 하나다(맨 `.zip`을 받으면 학생이 올린 사진 묶음까지 명렬에 뜬다). ③ **`accept`가 같은 자리에서 나온다** — 판정만 넓히면 거울상이 생긴다(**열 수는 있는데 고를 수 없는 파일**). 원본은 `rule-coverage.md`에 이미 적혀 있었다. **그리고 침묵을 따로 고쳤다**: 하나도 안 맞으면 조용히 빈 과녁으로 되돌아갔는데, 놓은 사람은 **아무 일도 안 일어난 것으로 본다** — 오늘 기기 로그를 끌어와야 했던 이유가 그것이다. **시작 화면의 프로젝트 열기는 되고 있었다**(이름을 안 본다) — 같은 파일이 화면마다 다르게 취급됐고, `0.26.0`의 범주형 산점도와 같은 모양이다. 돌연변이 셋을 심어 검사가 우는 것을 확인했다 |
| `0.26.4` | **감사 국면을 닫았다** — 기록은 `docs/audit/close-2026-09-23.md`. 지도(`map-2026-09-21.md` §3)에 쌓인 R35·R36·R37 지적을 한 번에 닫고, **결정이 걸린 여섯은 내가 고르지 않고 결정문 49~54로 세웠다.** 코드로 고친 것 셋: **취소당한 열기가 리다이렉트로 읽혀** 학생이 [점검]을 눌렀는데 목록이 섰고(`OpenOutcome` 세 갈래로 가른다), **남의 이름을 열쇠로 쓰는 자리 여덟**이 통로를 안 지났고(`src/records.ts`로 원시 연산을 꺼냈다 — `TRAINERS[algorithm]`은 진위 검사로 막고 있었는데 **상속한 함수는 참이라 `constructor`가 든 파일이 `Object`를 학습기로 불러 쓴다**), **`{ student: undefined }`**가 키를 남겼다(그런데 그 `undefined`가 실제로 **지우는 일**을 하고 있어서 키째 떼는 쪽으로 처방이 바뀌었다). **나머지는 전부 그물이다** — 검사 +74. 큰 것은 **스펙이 등록부의 배선을 안 지나고 같은 식을 손으로 다시 짜고 있던 자리**다(R35 B-1): sklearn 직렬화기의 인자를 바꿔도 대조가 조용했고, **같은 식을 두 번 쓰면 한쪽이 틀려도 둘이 맞는다**. **세 번은 재고 나서 겨냥을 옮겼다** — 감사자의 한 줄 불변식이 내 층 모양에서 깨졌고(1.2002 > 1.2), *"잠금이 샌다"*를 겨눈 판이 지워도 안 새서 **그 줄이 막는 것이 누출이 아니라 쓸데없는 읽기**임을 알았고, CP949 이름이 UTF-8로도 읽히는 **우연이 실재하는지**를 세어 899쌍을 찾았다. **내 지난 보고 하나는 철회했다** — *"조용한 돌연변이 둘"*은 내 되돌리기 뒤의 깨끗한 파일을 잰 것이었고 감사자가 맞았다 |
| `0.26.5` | **닫는 기록에서 빠뜨린 하나를 닫았다** — R36 C-6(준비 국면의 이음매). `0.26.4`의 기록이 닫은 목록에도 안 한 목록에도 이것을 안 적었고, **코드 소유자가 "전부 닫혔나"를 묻고서야 드러났다.** 핸들러가 내보내는 것과 클라이언트가 받는 것을 **따로** 재고 있어서, 양쪽이 함께 어긋나면(핸들러가 이름을 바꾸고 그 검사도 따라 고쳐지는 날) 두 판은 초록인 채로 **학생은 8.7초 동안 멈춘 화면을 본다.** 검사의 가짜 워커가 엔진 등록부를 받게 해서 진짜 핸들러가 낸 국면을 진짜 클라이언트가 받는 판을 세웠다. 앱 코드는 0줄이다 |
| `0.26.6` | **감사가 남긴 결정 여섯을 코드 소유자가 전부 정했다** (결정문 49~54). 셋은 현행 유지(49·51·54)이고, **54는 재 보고 닫았다** — ml.js가 자식 필드 이름을 바꾸는 드리프트를 심었더니 설치된 판으로 담았다 꺼내는 왕복 판 여섯이 울어서 판 번호를 못 박을 이유가 없었다. **코드로 바뀐 것 셋.** ① **옛 탭이 새 버전을 막지 않는다**(50) — 앱에 IndexedDB 연결을 닫는 코드가 한 줄도 없어서 `DB_VERSION`이 오르는 날 옛 탭을 열어 둔 학생의 새 탭이 영영 안 떴다. 이제 새 탭이 청하면 옛 탭이 스스로 놓고, 옛 탭이 그 뒤에 쓰려 하면 **이미 있는** `STORAGE_VERSION_TOO_NEW`가 말한다 — 새 문구 0개. ② **최빈값 동점은 sklearn을 따른다**(52) — sklearn 1.9.1을 직접 돌려 `서울·부산` 동점에서 `부산`, `10·9` 동점에서 `9.0`임을 쟀다. 우리는 먼저 나온 값이었고 같은 앱의 KNN 투표는 이미 sklearn 쪽이라 **동점 규칙이 둘**이었다. 수치 열은 문자열로 견주면 `10`이 `9`보다 작아지므로 수로 견주게 했다. ③ **열 종류를 그 실행이 쓰는 행 전체로 정한다**(53) — 같은 `모름` 한 칸이 훈련 몫이면 조용히 범주, 시험 몫이면 거절이라 **무작위 분할이 해석을 정했다.** pandas는 분할 전에 열 전체로 정하고 우리 데이터 화면의 요약도 그랬다(이 근거는 틀렸다 — 결정은 파일 전체가 아니라 실행의 행을 본다, R38 B-1에서 바로잡았다). 대가는 **같은 파일 시험 몫의 거절(R36 A-1)이 더 안 난다**는 것이고 결정문에 적었다. **그리고 dependabot은 메이저만 받는다** — PR 열 개가 전부 닫히고 머지가 0개였다. 문서상 `ignore`가 보안 PR에도 걸리는 것으로 읽혀서, 취약점 **알림**이 저장소 설정으로 켜져 있음을 확인하고 밀린 minor·patch는 감사 국면에 `npm outdated`로 본다 |

검사는 2,054개에서 **3,951개**가 됐다 (2026-09-23 실측).

**`0.9.0`에서 `RELEASED`를 `true`로 뒤집은 뒤로는, 파일에 적히는 어휘를 하나 바꾸면
`FORMAT_VERSION` 증가와 마이그레이션이 붙는다** (`tests/schema-version.spec.ts`).

**여기서 "어휘"는 화면 문구가 아니라 `.mlpx`에 그대로 적히는 코드값이다** —
`MISSING_STRATEGIES`의 `mean`, `SCALING_METHODS`의 `standard`처럼 스키마가 `z.enum`으로
막는 목록들이다. 파일 안에 `"scaling": "standard"`로 남으므로, 그 낱말을 바꾸거나 값을
하나 더하면 **이미 학생 손에 나가 있는 파일과 어긋난다.**

**로케일 문구는 여기 안 걸린다.** `표준화(Standardization)`라는 이름도, 그 설명도 파일에
안 적히므로 얼마든지 고칠 수 있다. 걸리는 것은 목록 자체가 늘거나 줄거나 이름이 바뀔
때뿐이고, 그때는 위 검사가 무엇을 해야 하는지까지 적어 준다.

**번호는 순서가 아니다.** 남은 단계의 실행 순서는 아래이고, 번호는 그 단계가 언제
세워졌는지를 말할 뿐이다. **번호를 다시 매기지 않기로 했다** (2026-08-15) — 자가호스팅과
음성·텍스트를 순서대로 옮기면 살아 있는 참조 스물다섯 곳이 함께 흔들리고, 이 저장소는
이미 번호가 두 번 흔들려 옛 결정문에 흉터가 남아 있다.

| 순서 | 단계 | 무엇을 |
|---|---|---|
| 끝 | **점검 (교사용 화면)** | **2026-09-18에 닫혔다.** 교사가 제출물을 열어 보는 화면 — 명렬 · 열람 · 무결성 · 재실행 대조 · 같은 프로젝트 묶기 · 포트폴리오 묶음 내려받기. **배포 뒤 목록에 가장 오래 있던 것**이고(2026-08-07에 V1에서 뺐다), 앞을 막던 결정 둘이 각각 풀렸다 — 허용 오차(#12)는 **정하지 않고 비켰고**(판정을 등록부가 갖고 못 가르는 자리는 교사에게 넘긴다), 이미지는 *못 하는 것*이 아니라 *아직 안 연 것*으로 뒀다. 설계는 계획 여덟 판과 감사 여섯 라운드, 구현 뒤 감사 세 라운드가 더 돌았다(`0.21.0`~`0.21.2`). 결정문 넷은 `open-decisions.md`, 화면의 모양은 `architecture.md` §8.21 |
| 끝 | **학습 병렬화** | **2026-09-04에 닫혔다.** 신경망·랜덤포레스트·KNN을 코어로 가른다 — 셋 다 **결과가 코어 수와 무관**하고, 그것을 `neural-parallel`·`forest-parallel`·`knn-parallel` 셋이 못 박는다. 신경망만 합산 정본이 바뀌어 엔진 2→3이고 나머지 둘은 비트 동일이다. 결정문은 `open-decisions.md` "학습을 코어로 가른다 — 결과는 코어 수와 무관하다" |
| 끝 | **두 탭 잠금** | **2026-09-04에 닫혔다.** 같은 프로젝트를 두 번째 탭이 못 연다 — Web Locks 우선, 비보안 컨텍스트(도커 http)는 BroadcastChannel. 결정문(2026-08-05)만 있고 코드가 0줄이던 자리였다 |
| 끝 | **V11 · R6~R9** | 코드 감사 — **2026-08-20에 닫혔다.** 라운드마다의 결과는 아래 V11 절에 |
| 끝 | **V10** | 랜딩과 행정 — **2026-08-26에 닫혔다.** 규정 문서가 앱과 함께 나간다 |
| 끝 | **V12** | **결정된 대기열** — **2026-09-01에 닫혔다.** 상한 off 스위치와 예상 시간이 나갔고, 남은 셋은 코드가 아니라 **실측**이라 아래 `재야 할 것`으로 옮겼다 |
| 끝 | **데이터 시각화** | **2026-09-22에 닫혔다.** 데이터 화면에서 열을 골라 그림으로 본다 — 히스토그램 · 막대그래프 · 박스 플롯 · 산점도. **표 경로에만 있다.** 결정문은 `open-decisions.md` "44. 데이터 화면의 시각화", 화면의 모양은 `architecture.md` §8.9.1 |
| 다음 학기 | **V6** | 자가호스팅 백엔드 (도커) — **2026-08-26에 미뤘다** |
| 다음 학기 | **V8 → V9** | 음성 분류 → 텍스트 분류 — V6과 함께 간다 |

**이름 붙은 단계가 비었다** (2026-08-26). 남은 것은 로드맵이 아니라 **대기열**이고,
그 목록은 V5 절의 "배포 뒤로 넘긴 것"과 미결정 항목들이 갖는다. 우선순위는 하나로
정한다 — **지금 학생이 무엇을 잃는가.** 그 기준에서 맨 앞은 세 가지다.

1. ~~**[멈추기]가 이미 끝난 모델을 버린다**~~ — **2026-08-26에 닫혔다.** 멈춰도 끝난
   모델이 실험으로 남는다. 결정문은 `open-decisions.md` "멈추기가 끝난 것을 남긴다".
2. ~~**내보내기 출력 스트리밍**~~ — **2026-08-27에 닫혔다.** 나가는 것이 `Blob`이고
   완성된 배열을 안 만든다. 결정문은 `open-decisions.md` "상한은 누가 정했느냐로 갈리고,
   우리 기기가 정한 것은 끌 수 있다" §4 — **`showSaveFilePicker`는 아직 안 붙였다.**
3. ~~**`#32` 파일 크기 알림**~~ — **2026-08-27에 닫혔다.** 막지 않고 알린다.
   결정문은 `open-decisions.md` "32. 프로젝트 파일 크기 경고".

**셋이 다 닫혔다** (2026-08-27). **그다음 줄을 2026-08-29에 세웠고 그것이 V12다.**

**대기열이 표 밖에 있으면 없는 것이 된다** (2026-08-29). 이름 붙은 단계가 빈 사흘 동안
남은 일 둘은 V5 절의 "배포 뒤로 넘긴 것"과 미결정에 흩어져 있었고, **머리의 표만 보는
사람에게는 남은 일이 없어 보였다** — 실제로 그렇게 읽혔다. 그래서 규칙을 하나 세운다:
**결정문이 있고 구현이 없는 것은 단계로 세워 이 표에 올린다.** 자리는 V12이고, 우선순위
기준은 그대로다 — **지금 학생이 무엇을 잃는가.**

**감사가 남긴 빚 중 첫째를 2026-08-19에 갚았다** — R1 A-1(백본 입력 범위). `FORMAT_VERSION`
2와 이 저장소의 **첫 마이그레이션**이 함께 왔다 (mlpx-spec.md §9.1). **V10 앞에 둔 이유가
순서였다** — V10이 학교 보급의 문을 여는 단계라, 그 문이 열리면 손 밖으로 나가는 파일이
늘고 마이그레이션이 못 되돌리는 숫자도 함께 는다.

**빚 셋이 2026-08-19에 전부 닫혔다** — A-1 구현 · 상한 결정문 넷 · 결정이 걸려 안 고친
지적 다섯 (V11 절 "감사가 남긴 빚"). **남은 것은 그 결정문들이 부른 구현이고**, 그건 빚이
아니라 배포 뒤 목록 옆에 서는 일이다.

**배포 뒤 목록은 이 순서 바깥에 있다** — V5에서 선을 그어 넘긴 것들이고 언제든 꺼낸다.
맨 앞이던 **이미지 총 용량 상한은 2026-08-20에 닫혔다** — 새 상한을 만드는 것이 아니라
`ensureRoom`이 묻는 시점을 굽기 전으로 당기는 것이었다 (`open-decisions.md` "이미지가
들어갈 자리는 굽기 전에 묻는다"). **이미지 테스트용 zip도 2026-08-28에 닫혔다** (V5 절) — 입구는 하루 먼저 섰고 학습까지 잇는 배선이 그날 왔다.

**배포 뒤 목록의 `교사용 화면`은 이제 `점검`이고, 2026-09-18에 설계가 닫히고 같은 날
나갔다** (`0.21.0`, 감사 뒤 `0.21.1`~`0.21.2`). 계획 여덟 판과 적대적 감사 여섯 라운드를
거쳐 결정문 넷이 섰다 — `open-decisions.md`의 "점검은 읽기 전용 열람기다 — 대조는 그
안의 한 판이다" · "명렬은 메타만 읽는다 — 입구는 둘, 경로는 하나" · "재실행은 학습
경로를 그대로 탄다" · "재현 판정은 (알고리즘 × 엔진)이 정하고, 못 가르는 자리는 교사에게
넘긴다". 화면의 모양은 `architecture.md` §8.21이 갖는다.

**선 것은 이렇다.** 폴더째 고르면 명렬이 표로 서고(입구 둘, 경로 하나), 한 줄을 고르면
**결과 화면의 부품 그대로** 열람·무결성·재실행 대조가 붙는다. 열람은 모델과 포트폴리오
두 모드이고 모드는 제출물을 건너 유지된다. 같은 `projectId`를 가진 파일을 묶어 보여 주되
**판정이 아니라 사실만 말하고**(교사가 나눠 준 시작 파일이면 반 전체가 같다), 서른 명의
글을 한 묶음으로 내려받는다. 원본 `.mlpx`는 어느 경로로도 안 고친다.

**구현 뒤 감사가 세 라운드 더 돌았다** (R28 · R28-V · R28-F, `0.21.1`~`0.21.2`).
A 2 · B 9 · C 23에 재검증이 꺼낸 새 결함 하나까지 전부 닫았고, 뿌리는 하나였다 —
**그물이 일이 아니라 이름을 본다.** `ui-rules`의 "워커를 여는 화면"이 사진 워커 이름
둘만 훑어 대조 판이 통째로 면제였고, `retire`가 없어 `alive()` 가드 셋이 **죽은 채로
초록**이었다. 지금 그 규칙의 기준은 `useWork()`를 부르는가다.

**위와 아래의 `교사용 화면`은 그때의 이름이라 그대로 둔다** — 날짜 박힌 결정과 순서
사슬이라 역사를 다시 쓰지 않는다. 이름의 대응은 `terms.md`의 `점검 (Inspect)`이 갖는다.
**미결정 12(허용 오차)는 더 이상 이 화면을 막지 않는다** — 판정 낱말이 정해져 설계가
실측 없이도 완결된다.

## 단계별 기록

**지금 무엇을 하는지는 위에 있다.** 아래는 단계마다의 기록이고, 파일이 갈려도
단계 이름은 그대로다.

### 닫힌 단계 — V1~V5

[`roadmap/01-v1-v5.md`](roadmap/01-v1-v5.md)

- **V1 (MVP) — 표 데이터 분류와 회귀**
- **V2 — 감사**
- **V3 — 군집화 (K-평균)**
- **V3와 V4 사이 — 잠긴 카드에 다음 수를 준다 (2026-08-12에 끝났다)**
- **V4 — 이미지 분류와 군집 — 2026-08-14에 닫혔다**
  - 0단계 — 종류의 경계를 먼저 긋는다 (이미지 코드가 한 줄도 없는 순수 리팩터)
  - 1단계 — 백본을 무엇이 실행하는가 (#25 ①) — 2026-08-12에 닫혔다
  - 1.5단계 — 백본을 붙인다
  - 2단계 — 이미지
- **V5 — 미뤄 둔 완성도 — 2026-08-15에 닫았다**

### 배포와 남은 단계 — V6·V7·V8·V10

[`roadmap/02-v6-v10.md`](roadmap/02-v6-v10.md)

- **V6 — 자가호스팅 백엔드 — 배포 뒤로 갔다 (2026-08-15)**
- **V7 — 실물 .mlpx 감사, 그리고 배포 — 2026-08-15에 닫혔다**
  - 실물 파일 감사는 끝났다 (2026-08-15)
  - 배포했다 (2026-08-15)
- **V8 이후**
- **V10 — 랜딩과 행정 — 다음 단계다 (2026-08-15)**

### V12 — 결정된 대기열

[`roadmap/04-v12.md`](roadmap/04-v12.md)

- **V12 — 결정된 대기열 — 2026-09-01에 닫혔다 (2026-08-29에 열었다)**
  - ~~**상한 off 스위치와 학습 예상 시간 — 짝이라 함께 낸다**~~ — 예상 시간은 `0.13.0`으로,
    **스위치는 2026-09-01에 나갔다.** 짝의 한쪽이 **사진에는 아직 없다** (아래 줄)
  - **알고리즘 상한을 다시 잰다 — 이제 시간이 아니라 실패를 잰다** (2026-09-01,
    코드 소유자). `MLJS_*_ROW_LIMIT` 여덟이 전부 **시간**으로 정해져 있는데, 그건
    예상 시간이 없던 시절의 보호막이다. **막을 것은 느린 것이 아니라 실패하는 것**이고
    (open-decisions.md "그러면 상한은 시간으로 정하는 것이 아니다"), 느린 것은 예상이
    말하고 학생이 정한다. 재는 도구는 `frontend/tools/bench.html`이다.
    **셋을 2026-09-01에 올렸다** — SVM 3,000→8,000 · KNN 10,000→50,000 ·
    트리 20,000→50,000. **랜덤포레스트만 남았고**, 그것을 재려면 하니스를 워커로
    옮기는 편이 낫다 — 지금은 메인 스레드라 "브라우저가 멈춘다"와 "계산이 실패한다"가
    구분되지 않는다. **그 이사를 2026-09-01에 했다** — 점마다 워커를 새로 띄우고,
    못 끝낸 방식을 셋으로 갈라 적는다
  - **사진 칸 일곱도 같은 질문을 받는다** (2026-09-01). `MLJS_IMAGE_*_ROW_LIMIT`은
    표 쪽을 다시 재는 동안 손대지 않아 **2026-08-14의 근거 그대로** 남았고, 그 근거도
    시간이다(*"500장 113초"*). **하니스에 사진 사다리가 하나도 없던 것이 이유다** —
    재려면 코드를 먼저 써야 했다. 사다리 일곱을 세웠고(`tests/bench-rules.spec.ts`가
    이제 등록부의 칸마다 사다리를 요구한다), 남은 것은 실측이다
  - **K-평균의 10만 행은 그대로 둔다** (2026-09-01, 코드 소유자). 평가에 천장이 생겨
    상한이 견딘다
- **닫은 근거 — 남은 것이 코드가 아니라 실측이다 (2026-09-01)** — 이 절의 입회 조건은
  *"결정은 섰는데 코드가 없는 것"*이었고, 남은 셋(랜덤포레스트 상한 · 사진 칸 일곱 ·
  사진 기준표)은 하니스와 사다리가 다 서서 **돌리는 일만 남았다.** 여기 두면 영영 안
  닫히는 칸이 되므로 위 **`재야 할 것`**으로 옮겼다

### V11 — Fable 코드 감사

[`roadmap/03-v11.md`](roadmap/03-v11.md)

- **V11 — Fable 코드 감사 — 2026-08-19에 닫혔다**
  - 감사가 남긴 빚 — 순서표 앞에 있다
  - R6·R7 — 감사가 감사한 것을 감사한다 (2026-08-19)
  - R8 — 검사기의 경계 (2026-08-19)
  - R9 — 제출물이 지나가는 길 (2026-08-19)
  - R1 — 이미지 경로 (2026-08-17에 끝났다)

## 명시적으로 범위 밖

- 사용자 계정 / 로그인 / 서버 측 프로젝트 목록
- 실시간 협업 편집
- 워크플로 노드 편집기 (Orange3 방식을 따라가지 않는다)
- **투영 계열 시각화** (FreeViz · Linear Projection · Radviz)와 **바이올린 플롯** —
  `open-decisions.md` "44. 데이터 화면의 시각화"가 왜 접었는지를 갖는다
- 모델 파일의 **세션을 넘어선 서버 보관** (모델은 클라이언트로 내려보내고 `.mlpx`에 담긴다)
- **우리가 운영하는 공용 백엔드** (CLAUDE.md §1.1 · `architecture.md` §7.1)
- **HMAC 서명·전자서명 기반 무결성** (`mlpx-spec.md` §7.1)

---

## 구현 순서 (권장)

포맷과 규칙이 흔들리면 그 위에 쌓은 모든 게 흔들린다. UI보다 먼저 만든다.

1. **저장소 골격** — 디렉터리, `pyproject.toml`, `package.json`, `.gitignore`, `docker-compose.yml`
2. **i18n 기반** — `errors.py`(StrEnum + `AppError` + 핸들러), 로케일 파일, `i18n.ts`, CI 검사
3. **`.mlpx` 포맷** — `schema.ts` / `format.ts` / `migrate.ts` / `storage.ts` + 왕복 테스트 ✅
4. **표 파일 파싱** — CSV·엑셀 읽기, 시트 선택, 인코딩 판정 ✅
5. **무결성 해시** — `hashes.json`(엔트리별 + `contentHash`), 재실행 대조의 토대 ✅
6. **브라우저 학습 엔진** — 여기가 V1의 심장이다. 순수 JS 완료, 남은 것은 아래 둘
   - 분할 / 전처리 / 알고리즘·지표 등록부 / ml.js 엔진 ✅
   - 실험 실행 — 엔진 등록부 + `runExperiment`. 지표까지 나온다 ✅
   - **모델 직렬화** ✅ — 형식·해석기 등록부와 **일곱 형식 전부**
     (`mlpx-tree-v1`·`linear-v1`·`linear-v2`·`naive-bayes-v1`·`linear-regression-v1`·
     `reference-v1`·`svm-v1`). #21의 부채 셋도 닫혔다
   - **Web Worker 껍데기** — 순수 함수들이 워커 뒤에 있다 ✅ (`ml/worker/`).
     부르는 화면은 학습 화면과 함께 온다
   - scikit-learn(Pyodide) **어댑터 등록은 V3에서 끝났다** — K-means와 함께 붙여
     실행 위치 축이 처음으로 실물 둘을 가졌다. **워커 배선은 V6 직전이다**(2026-08-12에
     내렸다. 이유는 위 V3 절)
7. **프런트엔드 UI** — 아래 순서로, 화면 하나씩. 틀은 `architecture.md` §8
   - **셸** — 도구 막대 / 레일 / 작업 공간 / 상태 표시줄 ✅ (§8.6)
   - **프로젝트 목록·만들기·열기·`.mlpx` 저장·불러오기·자동 저장** ✅
   - **데이터 화면** — CSV·엑셀 불러오기, 시트 선택, 미리보기, 열 요약, 정본 확정 ✅
   - **UX 리팩터** ✅ (2026-08-05). 강의노트형이던 첫 UI를 작업실로 바꿨다. 색은 그대로
     두고 밀도와 크롬만 바꿨다 — 겉모습이 `styles/theme.css`의 토큰 뒤에 있어 값과
     컴포넌트 내부만 바뀌었다 (§8.4). 어두운 배색도 그 토큰 덕에 값 한 겹으로 끝났다.
   - **전처리 화면** — 타깃·특성·결측치·스케일링·인코딩·분할 ✅ (2026-08-05).
     **데이터 얘기만 담는다** — 기계학습 유형과 모델 선정은 학습 화면이다
     (`open-decisions.md` "기계학습 유형은 모델을 고르는 자리에서 고른다")
   - **학습 화면** ✅ (2026-08-05). 유형 → 모델 선정 → 실행 방법이 **한 카드 안의 세 축**이고
     서로를 좁힌다 (`architecture.md` §8.12). [학습하기]는 워커에서 돌고 진행 표시는 모델
     단위이며 취소가 된다. 끝나도 결과 화면으로 옮기지 않는다 — 설정을 바꿔가며 반복
     학습하는 것이 이 도구의 핵심 활동이다.
   - **결과 화면** ✅ (2026-08-06). 순위표가 아니라 **변경 이력**이다 — 왼쪽 세로줄이
     학습 기록이고 오른쪽이 고른 실험의 속이다 (`architecture.md` §8.13). 지표 표시
     등록부와 변경 서술(`ml/metrics.ts`·`ml/changes.ts`)이 화면 밖에 있다
   - **결과 화면 2차 손질** ✅ (2026-08-07). 사람의 감사를 받고 고친 것들이다 —
     그 run에 먹인 하이퍼파라미터를 속 맨 위에(§8.13), 지표 이름과 혼동 행렬 칸을 눌러
     여는 용어 설명(수식 포함, §8.13), **특이도**를 범주별 점수에 추가(`mlpx-spec.md` §4),
     그리고 강조가 없는 차이를 지어내던 버그. 학습 화면도 함께 고쳤다 — 추가한 모델마다
     대기·학습 중·완료·실패를 보인다 (§8.17)
   - **데이터 화면의 열 검사기를 표 옆으로** ✅ (2026-08-06). 접어 두면 펼칠 때 표가
     먹혔다 (`architecture.md` §8.9). 같은 날 낮은 창에서 표가 잘리던 버그도 고쳤고
     규칙이 됐다 (§8.14)
   - **예측 화면** ✅ (2026-08-06). 왼쪽이 입력 한 줄, 오른쪽이 모델들의 답이다
     (`architecture.md` §8.13.1). **아직 사람의 감사를 안 받았다.**
     - **순수 JS SVM** ✅ (2026-08-06). 솔버는 벤더링한 SMO이고(`ml/engines/svm-smo.ts`)
       모델은 `mlpx-svm-v1`이다. 상한이 알고리즘마다 갈리는 자리가 여기서 처음
       생겼고(`MLJS_SVM_ROW_LIMIT`), 수렴하지 못한 학습은 실패가 아니라 `run.warning`이다
       (`mlpx-spec.md` §5.9)
     - 화면보다 먼저 만들 **이음매 둘** ✅ (2026-08-06). `ml/predict.ts`의 훈련 행
       만들기와 입력 한 줄 벡터화, 그리고 파일에서 읽은 전처리기를 검증하는
       `parsePreprocessor`. 확인하는 것은 "던지는가"가 아니라 **두 이음매가 같은
       좌표계에 있는가**다 (`architecture.md` §8.13.1)
   - **여기까지가 V1이다 (2026-08-07).** 포트폴리오 화면·교사용 화면·키보드·패널 크기
     조절은 **V5로 옮겼다** — 앞에 결정이 걸려 있고, 그보다 모든 유형과 데이터 종류가
     실제로 도는 것이 먼저다
   - **등록부와 축** ✅ (2026-08-07). 데이터 종류 × 과제 유형 × 실행 위치를 등록부가
     선언하고 **축이 늘면 컴파일이 깨진다** (`architecture.md` §9·§10). 지표 상세 패널과
     전처리 작업 공간도 등록부에서 나온다. V3(군집)과 V4(이미지)가 이 장치를 처음 쓴다
   - **재실행 대조의 로직** ✅ (2026-08-06). `ml/reproduce.ts` — 파일의 설정과 분할로
     다시 돌려 지표를 견준다. 엔진을 넘지 않고, **허용 오차는 정하지 않는다**
     (`open-decisions.md` #12). 교사용 화면은 그 결정 뒤다
8. **자가호스팅 백엔드 (V6)** — `config` / `diskguard` / `lifecycle` / `quota` / `jobs` / `ml` / `ws`
   **이미지를 브라우저로 해 본 뒤다.** 없어도 되는 것을 먼저 만들지 않는다.
   **배포 뒤로 갔다** (2026-08-15) — 배포 전제가 아니다. 위 V6 절을 봐라.

```
프로젝트 목록·열기 → 데이터 업로드·미리보기 → 전처리(타깃·특성·다듬기·나누기)
→ 학습(유형·모델·진행 표시) → 결과 분석 → 예측 → 포트폴리오 편집 → 교사용 화면
```

**백엔드가 5번에서 8번으로 내려갔다.** 서버 없이 동작하는 것이 V1의 정의이므로,
서버를 먼저 만들면 "서버가 있어야 되는 기능"이 슬며시 생긴다.

---

## 원칙 이탈 점검 (작업이 길어질 때 주기적으로)

- 서버에 상태가 남는 코드가 생겼는가
- 백엔드가 자연어 문자열을 반환하는 곳이 있는가
- 프런트엔드에 `t()`를 거치지 않은 문자열 리터럴이 있는가
- 임시 파일이 삭제되지 않는 경로가 있는가
- 코드가 출처여야 할 값이 문서에 복사돼 있는가

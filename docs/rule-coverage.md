# 규약을 무엇이 지키는가

`CLAUDE.md`의 규칙 하나하나에 대해 **누가 그것을 지키는지**를 적는다. 셋으로 가른다.

| 구분 | 뜻 |
|---|---|
| **검사** | 어기면 `npm run ci`가 빨개진다 |
| **타입** | 어기면 컴파일이 깨진다 |
| **사람** | **아무것도 안 막는다.** 기억하는 수밖에 없다 |

> **이 문서의 값은 세 번째 목록에 있다.** 앞의 둘은 "안심해도 되는 범위"를 알려 주고,
> 세 번째는 **감사를 부를 자리**와 **검사를 새로 만들 자리**를 알려 준다.
>
> **검사가 무엇을 주장하는지 여기 옮겨 적지 마라.** 그건 스펙 파일이 유일한 출처이고,
> 옮겨 적는 순간 낡는다 (`CLAUDE.md` 머리말). 여기 적는 것은 **어느 파일이 그것을
> 맡는가**까지다.

작성 2026-08-12. 백엔드(V6)가 오면 절반이 바뀐다.

---

## 검사가 막는다

| 규약 | 어디서 |
|---|---|
| §1.2 브라우저가 저장소다 | `storage.spec.ts` · `autosave.spec.ts` |
| §1.3 `.mlpx` 왕복 무손실 · 마이그레이션 | `format.spec.ts` · `migrate.spec.ts` · `lifecycle.spec.ts` |
| §1.5 상한 초과 시 올바른 에러 코드 | `table.spec.ts` · `storage.spec.ts` · `format.spec.ts` |
| §2 알고리즘 선택에 `if/elif` 금지 | `algorithms.spec.ts`("분기 없이 늘어난다") |
| §2 "X는 Y에서만"은 X의 등록부에 | `algorithms.spec.ts` · `metric-panels.spec.ts` · `kinds.spec.ts` |
| §2 잠금은 gate 함수 하나, 이유 목록 반환 | `selection.spec.ts` · `ui-rules.spec.ts` |
| §2 `randomState`는 항상 저장하고 항상 쓴다 | `split.spec.ts` · `experiment.spec.ts` · `sample.spec.ts` · `mljs-kmeans.spec.ts` |
| §3 i18n 규칙 넷 전부 | `i18n-usage.spec.ts` · `locales.spec.ts` |
| §4 `any` 금지 | ESLint `@typescript-eslint/no-explicit-any` |
| §4 Tailwind 임의 값 금지 | `ui-rules.spec.ts`의 `RULES` |
| §4 가장 작은 글자가 `text-base` | `ui-rules.spec.ts`의 `RULES` |
| §4 오래 걸리는 버튼은 `action` | `ui-rules.spec.ts`("버튼이 두 번 눌리지 않는다") |
| §4 화면 규칙 나머지 | `ui-rules.spec.ts` — 무엇을 막는지는 그 파일이 답한다 |
| §4 배포 전 버전 동결 | `versions.spec.ts` · `schema-version.spec.ts` |
| §4 DOM 필요한 스펙은 스스로 밝힌다 | `ui-rules.spec.ts` |
| §4 무결성 해시 · 재실행 대조 | `integrity.spec.ts` · `reproduce.spec.ts` · `lifecycle.spec.ts` |
| 엔진 수치 정확성 | `sklearn-parity.spec.ts` — 옛 코드를 넣으면 빨개진다 |

**소스 전체를 훑는 검사가 여섯이다** — `ui-rules` · `i18n-usage` · `locales` ·
`versions` · `schema-version` · `kinds`. 그래서 "내가 만진 파일의 검사"라는 것이 없고,
`CLAUDE.md` §4가 **커밋마다 전부 돌리라**고 적은 이유가 이것이다.

## 타입이 막는다

| 규약 | 어떻게 |
|---|---|
| §2 축이 늘면 컴파일이 깨진다 | `ml/axes.ts` · `ml/algorithms.ts`의 `Record<축, …>` |
| §2 등록부에 줄만 더한다 | 등록부 타입이 모든 칸을 요구한다 |
| 상한 판정이 세는 대상 | `trainableRowCount`의 `nSamples`가 **필수 인자**다 (#22) |

## 사람이 지킨다 — 아무것도 안 막는다

**여기가 이 문서의 알맹이다.**

### 검사로 만들 수 있는데 아직 없는 것

| 규약 | 지금 상태 | 만든다면 |
|---|---|---|
| §4 벤더링 머리말 셋(출처·라이선스·바꾼 것) | `svm-smo.ts` 하나뿐이고 잘 적혀 있다. **다음 파일을 강제하는 것이 없다** | `ml/engines/`에서 우리가 안 쓴 의존성을 들인 파일을 찾아 머리말 세 줄을 요구 |
| §4 커밋 규칙(GPG · 경로 명시 · 도구 표기 금지) | 전적으로 기억에 의존 | `commit-msg`·`pre-commit` 훅 |
| §1.5 `limits.ts`가 유일한 출처 | **지금은 어긴 곳이 없다**(2026-08-12 확인). 다음에 누가 넣으면 아무도 모른다 | 소스에서 상한 꼴 리터럴을 찾는 검사 |

### 검사로 만들 수 없는 것 — 감사나 사람 눈이 봐야 한다

| 규약 | 왜 못 막나 | 누가 보나 |
|---|---|---|
| §2 용어 — 학계 낱말, 우리가 지어낸 말 금지 | 낱말이 옳은지는 기계가 모른다 | `docs/terms.md` + 감사 |
| §2 파이썬 관행(이름·순서·기본값) | sklearn과 대조는 사람이 한다 | 감사 |
| 화면 문구의 말투 | 구조는 막히고 **목소리는 안 막힌다** | 코드 소유자 (`docs/copy.md`) |
| 화면 생김새 · 빈 상태 · 로딩 · 에러 | 렌더 결과의 좋고 나쁨 | **코드 소유자가 자기 브라우저로** |
| 문서와 코드의 정합성 | 검사가 문서를 안 읽는다 | 감사 (V2 2단계가 그것이었다) |
| 원칙 이탈 (§6의 자문 목록) | "이게 서버에 상태를 남기는가" 같은 물음 | 감사 |

### 백엔드(V6)가 오면 생기는 것 — 지금은 대상이 없다

`CLAUDE.md` §4가 **반드시 다루라**고 못 박았는데 아직 코드가 없어서 검사도 없다.
**V6 착수 시 이 목록부터 검사로 만든다.**

- 백엔드 응답에 자연어 문자열이 하나도 없는지 (§1.4)
- 작업 완료·실패·취소·타임아웃 **모든 경로**에서 임시 파일이 삭제되는지
- 디스크 부족 시 업로드 거부 (§1.5)
- 세션이 끊기면 그 세션의 모든 것이 지워지는지 (§1.1)
- 백엔드 주소가 설정 가능해지지 않았는지 (§2)

---

## 검사가 구조적으로 못 잡는 것

초록인 CI와 **완벽하게 공존하는** 실패들이다. 이 저장소에서 전부 실제로 나왔다.

1. **조용히 틀린 숫자.** V2 감사가 셋을 찾았고(SMO 상계, 로지스틱 솔버, KNN 동점),
   전부 몇 달간 CI 초록이었다. **가장 무서운 실패 모양이 이것이다** — 크래시가 아니라
   학생 파일 안의 틀린 값이고, 아무도 눈치채지 못한다.
2. **화면 생김새.** V3에서 코드 소유자의 눈이 넷을 잡았다. 감사도 CI도 못 잡았다.
3. **가짜 검사.** `sort`가 stable이라 동점 규칙을 지워도 통과하던 검사가 있었다.
   **망가뜨려 우는지 확인하는 것으로 부족하고**, 픽스처가 그 규칙을 실제로 가르는지도
   봐야 한다.
4. **누락.** 검사는 우리가 생각해 본 것이 무너지는 것을 잡지, 생각 못 한 것을 알려주지
   않는다.
5. **검사끼리의 오염.** 공유 픽스처를 제자리에서 고치면 **격리 실행에서는 통과하고 전체
   실행에서만** 빨개진다 (2026-08-12, `tests/fixtures.spec.ts`가 그 뒤로 막는다).

**그래서 감사가 필요하고, 그래서 실제 수업 테스트가 아무것도 대체되지 않는다.**

# 핸드오버 — 결과 화면부터

기준 커밋: `1d8619c` (main, clean, `npm test` 초록 734 통과 / 2 건너뜀)

> **이 파일은 임시다.** 다음 세션이 읽고 나면 갱신하거나 지워라. 오래 남을 내용은 전부
> `docs/`에 들어가 있고, 여기 있는 것은 "지금 어디까지 왔나"뿐이다.

## 먼저 읽어라

`CLAUDE.md` → `docs/roadmap.md` 구현 순서 7 → `docs/architecture.md` §8 →
`docs/open-decisions.md` → **`docs/copy.md`** (문구를 쓰거나 고칠 때)

## 지난 세션에 무엇이 바뀌었나

**학습 화면이 끝났다.** 그리고 사용자가 브라우저를 직접 조작하며 **화면 감사**를 했다.
커밋 20여 개 중 대부분이 그 감사의 결과다.

- **[학습] 버튼** — 워커 실행 · 모델 단위 진행 게이지 · 취소 · 이탈 경고 대화상자.
  결과는 `applyExperiment`(`project/attach.ts`)가 파일에 앉히고 자동 저장이 받는다.
  **끝나도 결과 화면으로 안 옮긴다** — 반복 학습이 핵심 활동이라 화면이 튀면 안 된다.
- **세 축이 한 카드에** — 기계학습 유형 · 모델 · 어디서 학습할까가 서로를 좁히는
  미니 카드 격자다 (`architecture.md` §8.12, 판정은 `ml/selection.ts`의 `modelAxes`).
- **문구 정제는 실패했고 통째로 되돌렸다** (`1d8619c`). 아래 "밀린 것" 참조.
- 그 밖에 감사에서 나온 것들: 버튼 상자 통일 · 대시보드 격자 정렬 · 표 세로 스크롤 ·
  첫 화면 튐 · 팝오버 간격 · 머리의 값 굵게 · `자체 서버` 라벨 · `바로가기`.

## 다음에 할 일

1. **결과 화면** ← **지금 가장 값이 큰 작업이다.** 지금은 학습이 되는데 **결과를 못
   보여주므로 수업 한 차시가 성립하지 않는다.** 이 화면 하나가 붙으면
   "데이터 → 학습 → 결과 분석" 45분이 완성되고 교실에 넣어 볼 수 있다.
   - **순위표가 아니라 변경 이력이다** (§8.9). `experiment.changed`가 이미 있다.
   - **분할을 끈 실험에는 "학습에 쓴 데이터로 매긴 점수"를 지표 옆에 붙여야 한다.**
   - 실패한 run은 접어서 보여준다. `failure.params.detail`에 원문이 있다.
2. **예측 화면 + 나머지 모델 형식 셋** — 한 덩어리다. 예측 화면이 `loadModel`을 부르는
   순간 참조형이 무엇을 필요로 하는지가 사실이 된다 (`open-decisions.md` #21).
   **착수 전에 문구 정제를 먼저 한다** (아래).
3. 포트폴리오 화면 → 상태 점검 패널(서버·엔진·저장소) → 교사용 화면
4. 키보드 단축키 · 패널 크기 기억 — 배포 직전
5. **첫 배포 커밋에서** `tests/schema-version.spec.ts`의 `RELEASED`를 뒤집는다.
   그전까지 `formatVersion`도 `MLJS_ENGINE.version`도 올리지 마라.

## 밀린 것 둘 — 내가 먼저 꺼내야 한다

**① 문구 전면 정제 — 예측 화면 착수 전.** 방식이 정해져 있다.

- `ko.json`만 고치고 **커밋하지 않는다.** `en.json`은 손대지 않는다.
- 사용자가 IntelliJ에서 **행 단위로 골라** `ko.json`을 커밋한다.
- 그 커밋을 읽고 내가 `en.json`을 맞춘다.
- **하위 에이전트에 맡기지 마라.** 지난 세션에 그렇게 했다가 되돌렸다 — 구조 규칙은
  완벽했는데 **이미 정해진 낱말**(`용량`, 병기, `바로가기`)을 바꿨다. 용어집도 만들지
  마라(`CLAUDE.md`의 "사실은 여기 적지 않는다"를 어긴다).

**② 전처리 결과 미리보기.** `켜고 끄면서 결과가 어떻게 달라지는지 보세요`가 지금은
빈 약속이다. 사용자는 이것을 **모델 점수가 아니라 데이터가 어떻게 변하는지**로 읽었고,
그쪽이 맞다. 재료는 있다 — `fitPreprocessor`·`transform`이 순수 함수이고 학습이 쓰는
바로 그 함수다. 착수 전 정할 것: 몇 줄을 보일지, 원-핫으로 늘어난 열을 어떻게 보일지,
**전처리기를 학습셋으로만 적합시킨다는 사실을 화면이 말할지.**

## 사용자가 답을 기다리는 것

**순수 JS SVM을 넣을지.** 조사와 실측이 끝났고 `open-decisions.md` #23에 전부 있다 —
**다시 재지 마라.** 요약: 넣을 수 있다, 특성 여러 개도 문제없다, 다중 클래스는 우리가
one-vs-one으로 감싸야 한다, 2000행에 10.4초라 알고리즘별 행 상한이 필요하다.

## 지뢰 (다시 밟지 마라)

지난 세션에 **실제로 밟은 것들**이다. 앞의 넷은 그날 새로 밟았다.

- **Vue 반응형 프록시는 구조화 복제가 안 된다.** 스토어에서 꺼낸 값을 그대로
  `postMessage`에 넘기면 `DataCloneError`가 **동기로** 난다. `toRaw`를 조각마다 한 번씩
  벗긴다(`composables/useTraining.ts`의 `plain`). 한 겹만 벗기면 안 된다.
  그리고 **던질 수 있는 호출을 `try` 밖에 두지 마라** — 상태가 안 풀려 화면이 갇힌다.
- **라우트 전환에 `<Transition>`을 넣지 마라.** 자식이 루트 **하나**여야 하는데 어기면
  작업 공간이 통째로 비고 `<!---->`만 남는다. **새로고침하면 멀쩡해서 원인을 못 짚는다.**
  `tests/ui-rules.spec.ts`가 이제 화면 루트 수를 검사한다.
- **버튼 변종은 전부 테두리를 갖는다** (안 보이면 `transparent`). 없으면 2px 차이로
  나란한 버튼의 줄이 어긋난다. 이것도 검사가 막는다.
- **세로 가운데 정렬 화면에 늦게 도착하는 내용을 붙이지 마라.** 블록이 자라면서 가운데가
  다시 잡혀 위로 튄다. 첫 읽기가 끝난 뒤에 한 번에 그린다(`WelcomeView`).
- **`AppTable`은 이제 세로로도 스크롤된다.** `sticky` 머리가 그 전제였다.
- **배포 전에는 버전을 올리지 마라.** `formatVersion`도 `MLJS_ENGINE.version`도.
- **키를 옮기면 참조가 남는다.** `locales.spec.ts`가 소스의 정적 `t('a.b')`를 훑어 막는다.
- **`RouterLink`에 `route.params`를 쓰지 마라.** 가드가 여는 시점에는 주소가 옛것이다.
  잠긴 칸도 `RouterLink`로 두지 마라. 출처는 스토어다.
- **정리를 화면 생명주기에 맡기지 마라.** 라우터 가드가 한다.
- **네이티브 `popover` 속성은 앵커가 안 된다.** `AppPopover`가 `absolute`로 붙인다.
- **칸의 안쪽 폭이 상태에 따라 달라지면 안 된다.** 테두리는 늘 두고 색만 바꾼다.
- **vitest에서 `vi.useFakeTimers()`를 통째로 켜면 fake-indexeddb가 멈춘다.**
- **IndexedDB 테스트에서 레코드를 직접 심을 때**는 앱 경로로 한 번 저장해 두어라.
- **셸의 작업 디렉터리가 리셋된다.** `frontend`에서 도는 명령은 매번 `cd`를 붙여라.
  파이썬은 `/tmp`를 못 쓴다 — 스크래치패드 경로를 써라.

## 검사가 막아 주는 것 (사람이 지키길 기대하지 않는다)

`tests/ui-rules.spec.ts` — `text-sm` 금지 · 임의 값 금지 · 래퍼 `gap-5` · 버튼이
`action`으로 묶였는가 · **버튼 변종의 테두리** · **화면 루트가 하나인가**
`tests/locales.spec.ts` · `tests/i18n-usage.spec.ts` — 키 집합 · 자리표시자 · 조사 ·
문장 조각 잇기 · 코드마다 문장이 있는가

**검사기 자체를 먼저 검사한다.** 새 검사를 넣으면 **고치기 전 코드로 되돌려 빨개지는
것까지 확인하라.** 지난 세션에 셋 다 그렇게 확인했다.

## 커밋 규칙 (CLAUDE.md §4, 예외 없음)

- `main`에서 순차적으로. 기능 브랜치 금지.
- **GPG 서명 필수.** 실패하면 우회하지 말고 멈추고 보고하라.
- 커밋 메시지에 도구 표기(`Co-Authored-By` 등) 금지.
- **스테이징 경로를 명시하라.** `git add .` / `-A` / `commit -a` 전부 금지.
- 커밋 전에 `npm run lint && npm run typecheck && npm test`. **`frontend`에서.**
  **`typecheck`를 파일 고친 뒤에 다시 돌려라** — 지난 세션에 안 돌리고 초록이라고 보고했다가
  테스트 파일의 타입 오류 셋을 흘렸다.

## 작업 방식

- **사용자가 화면을 직접 보고 감사한다.** 브라우저를 켜지 말라고 했다 — 스크린샷과
  수정 요청이 오면 코드로 고치고, 눈으로 확인 못 한 것은 **확인 못 했다고 말하라.**
- 결정이 걸린 작업은 `docs/`를 먼저 고쳐 커밋한 뒤 구현한다.
- 계획을 먼저 제시하고 판단을 받는다. 갈림길은 선택지로 물어라.

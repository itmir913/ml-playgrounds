/**
 * **앱 밖으로 나가는 주소.** 여기가 유일한 출처다.
 *
 * `legal.ts`와 갈리는 자리다. 그쪽은 **전부 상대 경로**다 — 규정은 그 배포가 하는
 * 약속이라, 절대 주소를 박으면 학교가 직접 띄운 자가호스팅이 남의 사이트의 방침을
 * 가리킨다.
 *
 * **여기는 반대다.** 원본이 하나뿐이고 자가호스팅에서도 **같은 파일**을 받아야 한다.
 * 상대 경로로 걸면 도커로 띄운 학교에서 404가 된다. 그래서 집을 갈랐다 — `legal.ts`에
 * 두면 그 파일의 "전부 상대 경로다"가 거짓이 되고, 다음 사람이 규칙대로 고친다.
 *
 * 근거는 `open-decisions.md` "예시 데이터셋은 바깥에 있고, 앱은 주소만 갖는다".
 */

/**
 * 예시 데이터셋 서랍. **파일 자체는 산출물에 안 들어간다.**
 *
 * 전국 배포이고 컴퓨터실 PC는 차시마다 리셋되므로, `dist/`에 실으면 그 값을
 * **예시를 안 쓰는 학생까지 낸다** (`open-decisions.md` "백본을 붙이는 방법"의 계산).
 * 원본이 서빙하고 우리는 링크만 건다.
 */
export const DEMO_DATASETS_URL =
  'https://luminousky.com/teacher-utility-kit/ml-playgrounds/datasets/'

/**
 * 이 프로젝트의 저장소. **첫 화면 아래에서 `개인정보 처리방침`·`오픈소스 라이선스`와
 * 나란히 선다.**
 *
 * `legal.ts`가 아니라 여기인 이유가 이 파일 머리말 그대로다 — 저장소는 어디서 띄우든
 * **한 곳**이라, 학교가 직접 띄운 앱에서도 같은 주소를 가리켜야 맞다.
 */
export const REPOSITORY_URL = 'https://github.com/itmir913/ml-playgrounds'

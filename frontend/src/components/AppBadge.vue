<script setup lang="ts">
/**
 * 회색 알약 (architecture.md §8.13 "이름은 배지, 값은 plaintext다").
 *
 * **여기 담기는 것은 언제나 "이름"이다** — 어느 모델의 것인지, 어떤 설정인지, 지금
 * 어떤 상태인지. **바뀐 값이나 측정값은 안 담는다.** 값까지 배지에 넣으면 목록에서
 * 무엇이 이름이고 무엇이 결과인지가 다시 흐려지고, 그러면 이 배지를 만든 이유가 사라진다.
 *
 * **컴포넌트로 두는 이유는 같은 알약이 여러 자리에 서기 때문이다** (§8.4). 클래스
 * 문자열을 자리마다 적으면 그중 하나만 고쳐지는 날이 오고, 어긋난 것을 아무도 못 잡는다.
 *
 * 색은 안 받는다. 뜻을 가진 색(학습 중 · 실패 같은 것)은 그 화면의 판정과 함께 있어야
 * 하고(§10.1의 등록표), 여기에 tone prop을 열면 그 판정이 이 컴포넌트로 새어 들어온다.
 */
</script>

<template>
  <span class="rounded-field bg-surface-sunken px-2 py-0.5 text-ink-soft">
    <slot />
  </span>
</template>

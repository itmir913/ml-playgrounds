/**
 * 브라우저 학습 구현. **V1의 기본 실행 위치다** - 공식 배포에는 서버가 없다.
 *
 * 엔진은 둘이다 - 순수 JS가 기본이고 scikit-learn(Pyodide)은 학생이 상태 점검에서 켠다
 * (docs/open-decisions.md "브라우저 학습 엔진은 둘 다 간다"). 실측으로 확인된 것 -
 * 순수 JS는 gzip 25KB에 시동이 없고, scikit-learn은 26.3MB에 시동만 15.4초다.
 * 그래서 **로딩 시점을 자동으로 정하지 않는다.** 교사가 부하 타이밍을 쥐어야 한다.
 *
 * 서명은 만들지 않는다. 무결성은 해시와 재실행 대조로 확인하며, 대조는 그 run을 만든
 * 엔진으로만 한다 - 엔진이 다르면 숫자가 실제로 갈린다(로지스틱 회귀 0.9667 대 0.8667).
 * 각 run에 engine을 기록하는 이유다 (docs/architecture.md 3.2).
 */

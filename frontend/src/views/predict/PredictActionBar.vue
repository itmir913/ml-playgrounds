<script setup lang="ts">
/**
 * 예측 화면의 동작 바 — **누르는 것이 전부 여기 모인다** (architecture.md §8.13.1
 * "동작 바는 세 경로가 함께 쓴다").
 *
 * **표·값, 표·파일, 이미지 셋이 이것 하나를 쓴다.** 표와 이미지가 갈려 있으면 한 교사가
 * 두 수업에서 다른 화면을 가르치게 된다. 자리는 둘이고 뜻이 정해져 있다 — 기본 자리에는
 * **입력을 채우고 비우는 것**이, `end` 자리에는 **그 모드의 결론** 하나가 선다(값과
 * 사진은 [예측]이고, 파일은 누를 예측이 없어 [내려받기]다).
 *
 * **위에 붙어 따라온다.** 답이 길게 이어지는 화면이라 스크롤하면 누를 것이 화면 밖으로
 * 나간다 — 학생은 아무 일도 안 일어난 줄 알고 다시 누르러 올라간다. 학습 화면이 동작을
 * 흐름 끝에 두는 것과 갈리는 이유가 이것이다(§8.9).
 *
 * **`fixed`가 아니다** — `AppShell`의 상태 표시줄이 `<main>` 밖에 있다.
 *
 * **바깥 칸은 여백을 만드는 자리다.** `top-0`으로 붙이면 바가 화면 맨 끝에 딱 달라붙어
 * 눌린 것처럼 보이고, 그렇다고 `top-4`로 띄우면 그 틈으로 아래 내용이 지나가는 것이
 * 보인다. 그래서 위쪽 여백만큼을 **칸 안의 `pt`로 넣고 같은 값을 `-mt`로 도로 빼서**,
 * 붙었을 때 그 자리를 불투명한 바탕이 덮게 한다 — 값은 화면의 바깥 여백(`PredictView`의
 * `p-4 sm:p-5`)과 같아야 제자리에 선다.
 *
 * **긴 문구를 여기 넣지 마라.** 바가 두 줄이 되면 아래 붙박이 열이 멈추는 자리가
 * 어긋난다(§8.13.1 "왼쪽은 붙박이다"의 계산). 여기 설 수 있는 말은 진행 표시처럼 짧은
 * 것뿐이다.
 */
</script>

<template>
  <div class="sticky top-0 z-10 -mt-4 bg-surface pt-4 sm:-mt-5 sm:pt-5">
    <div
      class="flex flex-wrap items-center gap-3 rounded-panel border border-line-strong bg-surface px-4 py-2.5 shadow-card"
    >
      <slot />

      <!-- 결론은 늘 오른쪽 끝이다. 자리를 컴포넌트가 정해야 세 경로가 안 갈린다. -->
      <div class="ml-auto flex flex-wrap items-center gap-3">
        <slot name="end" />
      </div>
    </div>
  </div>
</template>
